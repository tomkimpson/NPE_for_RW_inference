\documentclass{article}
%DIF LATEXDIFF DIFFERENCE FILE
%DIF DEL /tmp/template_main.tex    Fri Feb 27 13:06:36 2026
%DIF ADD docs/paper/template.tex   Thu Feb 26 18:44:43 2026

\usepackage{hyperref}                 % Hyperlinks
\hypersetup{
	hidelinks,                        % Hide link boxes
	colorlinks = true,
	citecolor = cyan,
	urlcolor = black,
	linkcolor = magenta
}
\usepackage{arxiv}
\usepackage{amsmath}
\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{cleveref}       % smart cross-referencing
%DIF 21c21
%DIF < \usepackage{lipsum}         % Can be removed after putting your text content
%DIF -------
% \usepackage{lipsum}       % Removed — placeholder package not needed %DIF > 
%DIF -------
\usepackage{graphicx}
\usepackage{natbib}
\usepackage{doi}
\usepackage{xcolor}
\usepackage{enumerate}% http://ctan.org/pkg/enumerate
\usepackage{float}
%DIF 28a28
\usepackage{multirow} %DIF > 
%DIF -------

\usepackage{subcaption}


%DIF 32c33
%DIF < \title{Rapid Parameter Inference for Spatiotemporal Stochastic Biological Models using Neural Posterior Estimation}
%DIF -------
\title{Likelihood-Free Parameter Inference for Spatiotemporal Stochastic Biological Models using Neural Posterior Estimation} %DIF > 
%DIF -------

%DIF 34c35
%DIF < \renewcommand{\shorttitle}{Amortized Inference for Stochastic Models}
%DIF -------
\renewcommand{\shorttitle}{Likelihood-Free Inference for Stochastic Biological Models} %DIF > 
%DIF -------



% Here you can change the date presented in the paper title
%\date{September 9, 1985}
% Or remove it
%\date{}


% Multiple affiliations variant of author block
\usepackage{authblk}
\renewcommand\Authfont{\bfseries}
\setlength{\affilsep}{0em}
% box is needed for correct spacing with authblk
\newbox{\orcid}\sbox{\orcid}{\includegraphics[scale=0.06]{orcid.pdf}}

\author[1,2]{%
	\href{https://orcid.org/0000-0002-6542-6032}{\usebox{\orcid}\hspace{1mm}Tom Kimpson\thanks{Corresponding author: \texttt{tom.kimpson@unimelb.edu.au}}}%
}
\author[1,2]{%
	\href{https://orcid.org/0000-0002-8809-726X}{\usebox{\orcid}\hspace{1mm}Jennifer Flegg}%
}
\author[2,3]{%
	\href{https://orcid.org/0000-0001-6254-313X}{\usebox{\orcid}\hspace{1mm}Matthew J.\ Simpson}%
}

\affil[1]{School of Mathematics and Statistics, The University of Melbourne, Parkville, VIC 3010, Australia}
\affil[2]{ARC Centre of Excellence for the Mathematical Analysis of Cellular Systems (MACSYS), The University of Melbourne, Parkville, VIC 3010, Australia }

\affil[3]{School of Mathematical Sciences, Queensland University of Technology, Brisbane, QLD 4001, Australia}



% Uncomment to override  the `A preprint' in the header
%\renewcommand{\headeright}{Technical Report}
\renewcommand{\undertitle}{}



\newcommand{\SP}{S\&P2025}

%%% Add PDF metadata to help others organize their library
%%% Once the PDF is generated, you can check the metadata with
%%% $ pdfinfo template.pdf
%\hypersetup{
%pdftitle={Amortized Inference for Stochastic Biological Models using Neural Posterior Estimation},
%pdfsubject={q-bio.NC, q-bio.QM},
%pdfauthor={David S.~Hippocampus, Elias D.~Striatum},
%pdfkeywords={First keyword, Second keyword, More},
%}
%DIF PREAMBLE EXTENSION ADDED BY LATEXDIFF
%DIF CFONT PREAMBLE %DIF PREAMBLE
\RequirePackage{color}\definecolor{RED}{rgb}{1,0,0}\definecolor{BLUE}{rgb}{0,0,1} %DIF PREAMBLE
\DeclareOldFontCommand{\sf}{\normalfont\sffamily}{\mathsf} %DIF PREAMBLE
\providecommand{\DIFaddtex}[1]{{\protect\color{blue} \sf #1}} %DIF PREAMBLE
\providecommand{\DIFdeltex}[1]{{\protect\color{red} \scriptsize #1}} %DIF PREAMBLE
%DIF SAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddbegin}{} %DIF PREAMBLE
\providecommand{\DIFaddend}{} %DIF PREAMBLE
\providecommand{\DIFdelbegin}{} %DIF PREAMBLE
\providecommand{\DIFdelend}{} %DIF PREAMBLE
\providecommand{\DIFmodbegin}{} %DIF PREAMBLE
\providecommand{\DIFmodend}{} %DIF PREAMBLE
%DIF FLOATSAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddFL}[1]{\DIFadd{#1}} %DIF PREAMBLE
\providecommand{\DIFdelFL}[1]{\DIFdel{#1}} %DIF PREAMBLE
\providecommand{\DIFaddbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFaddendFL}{} %DIF PREAMBLE
\providecommand{\DIFdelbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFdelendFL}{} %DIF PREAMBLE
%DIF HYPERREF PREAMBLE %DIF PREAMBLE
\providecommand{\DIFadd}[1]{\texorpdfstring{\DIFaddtex{#1}}{#1}} %DIF PREAMBLE
\providecommand{\DIFdel}[1]{\texorpdfstring{\DIFdeltex{#1}}{}} %DIF PREAMBLE
\newcommand{\DIFscaledelfig}{0.5}
%DIF HIGHLIGHTGRAPHICS PREAMBLE %DIF PREAMBLE
\RequirePackage{settobox} %DIF PREAMBLE
\RequirePackage{letltxmacro} %DIF PREAMBLE
\newsavebox{\DIFdelgraphicsbox} %DIF PREAMBLE
\newlength{\DIFdelgraphicswidth} %DIF PREAMBLE
\newlength{\DIFdelgraphicsheight} %DIF PREAMBLE
% store original definition of \includegraphics %DIF PREAMBLE
\LetLtxMacro{\DIFOincludegraphics}{\includegraphics} %DIF PREAMBLE
\newcommand{\DIFaddincludegraphics}[2][]{{\color{blue}\fbox{\DIFOincludegraphics[#1]{#2}}}} %DIF PREAMBLE
\newcommand{\DIFdelincludegraphics}[2][]{% %DIF PREAMBLE
\sbox{\DIFdelgraphicsbox}{\DIFOincludegraphics[#1]{#2}}% %DIF PREAMBLE
\settoboxwidth{\DIFdelgraphicswidth}{\DIFdelgraphicsbox} %DIF PREAMBLE
\settoboxtotalheight{\DIFdelgraphicsheight}{\DIFdelgraphicsbox} %DIF PREAMBLE
\scalebox{\DIFscaledelfig}{% %DIF PREAMBLE
\parbox[b]{\DIFdelgraphicswidth}{\usebox{\DIFdelgraphicsbox}\\[-\baselineskip] \rule{\DIFdelgraphicswidth}{0em}}\llap{\resizebox{\DIFdelgraphicswidth}{\DIFdelgraphicsheight}{% %DIF PREAMBLE
\setlength{\unitlength}{\DIFdelgraphicswidth}% %DIF PREAMBLE
\begin{picture}(1,1)% %DIF PREAMBLE
\thicklines\linethickness{2pt} %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\framebox(1,1){}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\line( 1,1){1}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,1){\line(1,-1){1}}}% %DIF PREAMBLE
\end{picture}% %DIF PREAMBLE
}\hspace*{3pt}}} %DIF PREAMBLE
} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbegin}{\DIFaddbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddend}{\DIFaddend} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbegin}{\DIFdelbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelend}{\DIFdelend} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbegin}{\DIFOaddbegin \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbegin}{\DIFOdelbegin \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbeginFL}{\DIFaddbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddendFL}{\DIFaddendFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbeginFL}{\DIFdelbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelendFL}{\DIFdelendFL} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbeginFL}{\DIFOaddbeginFL \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbeginFL}{\DIFOdelbeginFL \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
%DIF COLORLISTINGS PREAMBLE %DIF PREAMBLE
\RequirePackage{listings} %DIF PREAMBLE
\RequirePackage{color} %DIF PREAMBLE
\lstdefinelanguage{DIFcode}{ %DIF PREAMBLE
%DIF DIFCODE_CFONT %DIF PREAMBLE
  moredelim=[il][\color{red}\scriptsize]{\%DIF\ <\ }, %DIF PREAMBLE
  moredelim=[il][\color{blue}\sffamily]{\%DIF\ >\ } %DIF PREAMBLE
} %DIF PREAMBLE
\lstdefinestyle{DIFverbatimstyle}{ %DIF PREAMBLE
	language=DIFcode, %DIF PREAMBLE
	basicstyle=\ttfamily, %DIF PREAMBLE
	columns=fullflexible, %DIF PREAMBLE
	keepspaces=true %DIF PREAMBLE
} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim}{\lstset{style=DIFverbatimstyle}}{} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim*}{\lstset{style=DIFverbatimstyle,showspaces=true}}{} %DIF PREAMBLE
%DIF END PREAMBLE EXTENSION ADDED BY LATEXDIFF

\begin{document}
\maketitle





\begin{abstract}
\DIFdelbegin \DIFdel{Parameter inference for stochastic models is essential for quantitative biology, but is }\DIFdelend \DIFaddbegin \DIFadd{Cell migration is a key biological process underlying wound healing, tissue development, and cancer metastasis, yet calibrating mathematical models of migration to experimental data remains a major challenge. Scratch and barrier assays are widely used to study collective cell spreading, and agent-based random walk models provide a natural stochastic description of these experiments. However, parameter inference for such models is }\DIFaddend hampered by intractable likelihoods\DIFdelbegin \DIFdel{. Agent-based random walk models, used widely in ecology and cell biology, exemplify this challenge, often }\DIFdelend \DIFaddbegin \DIFadd{, }\DIFaddend forcing researchers to \DIFdelbegin \DIFdel{use likelihood-free methods like }\DIFdelend \DIFaddbegin \DIFadd{rely on }\DIFaddend Approximate Bayesian Computation\DIFdelbegin \DIFdel{(ABC) that introduce }\DIFdelend \DIFaddbegin \DIFadd{, which introduces }\DIFaddend biases and tuning difficulties, or \DIFdelbegin \DIFdel{else surrogate models which rely on a potentially unfaithful approximation and require an explicit, potentially erroneous , noise model }\DIFdelend \DIFaddbegin \DIFadd{surrogate models that require potentially erroneous noise model specifications}\DIFaddend . Here, we overcome these limitations using \DIFdelbegin \DIFdel{Neural Posterior Estimation (NPE)}\DIFdelend \DIFaddbegin \DIFadd{neural posterior estimation}\DIFaddend , a simulation-based \DIFdelbegin \DIFdel{, machine learning }\DIFdelend inference framework that learns the full posterior distribution \DIFdelbegin \DIFdel{. Once trained, NPE provides amortized, near-instantaneous posterior estimates. }\DIFdelend \DIFaddbegin \DIFadd{directly from stochastic simulations without surrogate approximations or explicit noise model specifications. }\DIFaddend We deploy this framework on \DIFdelbegin \DIFdel{a random walk model of a barrier assay experiment }\DIFdelend \DIFaddbegin \DIFadd{four progressively complex random walk models of barrier assay experiments }\DIFaddend describing in vitro cell migration\DIFdelbegin \DIFdel{. We demonstrate our approach }\DIFdelend \DIFaddbegin \DIFadd{: an isotropic baseline, a model with directional bias (chemotaxis), a model with cell proliferation, and a combined model incorporating both bias and proliferation. For each model, we demonstrate inference }\DIFaddend in two settings: \DIFdelbegin \DIFdel{first, }\DIFdelend using one-dimensional summary statistics (column counts), and \DIFdelbegin \DIFdel{second, by augmenting NPE with a novel }\DIFdelend \DIFaddbegin \DIFadd{using a }\DIFaddend convolutional neural network \DIFdelbegin \DIFdel{(CNN) that enables NPE to learn }\DIFdelend \DIFaddbegin \DIFadd{that enables inference }\DIFaddend directly from raw two-dimensional spatial data. \DIFdelbegin \DIFdel{In both cases, NPE remains the core inference engine, with the CNN serving as a feature extraction component for spatial data. On synthetic data, our NPE pipeline proves highly effective in both settings, delivering posterior estimates at a substantially reduced computational cost per inference. When applied to 1D summary statistics, the pipeline matches or exceeds the precision of classical methods. Similarly, for 2D spatial data, NPE augmented with CNN performs inference directly on raw images with a precision comparable to that from curated summary statistics. This second capability is critical, as it moves beyond the traditional reliance on lower-dimensional data and opens up new avenues for complex models where informative summary statistics are not obvious or available. We }\DIFdelend \DIFaddbegin \DIFadd{Neural posterior estimation performs well across all four models, recovering biologically interpretable parameters (e.g. cell motility, directional bias, proliferation rates) from cases where classical surrogate-based methods are adequate through to the combined model where the interplay of multiple mechanisms renders surrogate approximations unreliable. We validate all posteriors using simulation-based calibration diagnostics and }\DIFaddend provide an open-source implementation of our pipeline to facilitate its adoption and extension to more complex, spatially-structured biological models.
\DIFdelbegin \DIFdel{This work provides a practical framework for parameter inference in spatial stochastic models that leverages machine learning to avoid the need for likelihood functions, hand-crafted summary statistics, and approximate noise models.
}\DIFdelend \end{abstract}


% keywords can be removed
%\keywords{First keyword \and Second keyword \and More}



%DIF > % ============================================================
%DIF > %  1. INTRODUCTION
%DIF > % ============================================================
\section{Introduction}
\DIFdelbegin \DIFdel{Parameter inference is the critical link between mathematical models and biological understanding, enabling quantitative insights from noisy experimental data. Inference methods provide researchers with a powerful toolkit to generate predictions about system behaviour, estimate unknown parameters, test competing hypotheses, and rigorously quantify uncertainty. These applications are critical across a wide range of fields, including epidemiology \citep{Dunson2005,Semochkina2025}, ecology \citep{Qian2009,Rosen2001}, cellular biology \citep{Vallejos2015}, gene regulatory networks \citep{Liu2016,Wang2019}, systems biology \citep{Rabinowitz2013} phylogenetics \citep{Nascimento2017}, and neuroscience \citep{Coventry2024}. }\DIFdelend 

\DIFdelbegin \DIFdel{For deterministic continuum models described by ordinary and partial differential equations, inference methodologies are mature and well-established. Both Bayesian \citep{Bernardo1994,Gelman1995} and frequentist \citep{Cox1974,Casella1990} approaches provide robust foundations for parameter estimation, model validation, and uncertainty quantification. The sophistication of these inference techniques has enabled reliable parameter estimation and prediction across numerous scientific disciplines including climate science \citep{Vrugt2014}, cosmology \citep{Planck2018Params}, fluid dynamics \citep{Cheung2011} and gravitational wave astrophysics \citep{Veitch2015}. }\DIFdelend \DIFaddbegin \DIFadd{Scratch and barrier assay experiments are widely used to study how cell populations spread, proliferate, and interact in vitro \citep{Jin2016ScratchRepro,Johnston2014LeadingEdgeInfo,Simpson2013CircularBarrier}, with applications spanning wound healing, embryonic development, immune responses, and cancer metastasis \citep{Friedl2009,Rorth2009}. In these experiments, cells are confined to a region on a substrate; after barrier removal, the population spreads outward, and the resulting spatiotemporal dynamics encode information about the underlying motility and proliferation mechanisms. Extracting this information quantitatively requires calibrating mathematical models to experimental data, yet doing so remains a major challenge for stochastic, agent-based descriptions of cell migration.
}\DIFaddend 

\DIFdelbegin \DIFdel{Spatially explicit, stochastic biological models, however, present fundamental inferential challenges that resist these established approaches.
}\DIFdelend Agent-based \DIFdelbegin \DIFdel{models and discrete stochastic processes, while capturing essential }\DIFdelend \DIFaddbegin \DIFadd{random walk models on regular lattices provide a natural stochastic description of these experiments, capturing the inherent }\DIFaddend biological variability and heterogeneity \DIFdelbegin \DIFdel{, typically possess intractable likelihoods that preclude standard statistical methods. Experimental protocols exhibit substantial variation, datasets are frequently sparse and noisy, and the stochastic nature of biological processes introduces complex dependencies that deterministic frameworks }\DIFdelend \DIFaddbegin \DIFadd{that deterministic continuum models }\DIFaddend cannot adequately represent \DIFaddbegin \DIFadd{\citep{Codling2008,Simpson2025.05.25.656057,Plank2025}. However, calibrating these discrete stochastic models to data poses serious inferential challenges}\DIFaddend . \DIFdelbegin \DIFdel{These characteristics create a disconnect between the sophisticated stochastic models needed to represent biological reality and the inference tools available to calibrate them from data.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Agent-based models of cell migration and proliferation have become a critical test bed for developing new inference techniques that can preserve the biological realism of stochastic models \citep{Carr2021,SIMPSON2022111201,Simpson2025.05.25.656057,Ross2017ABCadhesion,Warne2022RapidBayes,Liu2024PRE,Lagergren2020BINN,Nardini2023BINNForecast}. These approaches often use experimental data such as scratch and barrier assay experiments to calibrate high-fidelity discrete random-walk models, highlighting applications where stochasticity is visible and decision-relevant \citep{Jin2016ScratchRepro,Johnston2014LeadingEdgeInfo,Simpson2013CircularBarrier}. At the same time, these successes have clarified several canonical challenges that have historically limited the broader application of stochastic models \citep{Liu2024PRE,Warne2022RapidBayes}. While these approaches demonstrate the potential of sophisticated inference frameworks, they also reveal fundamental limitations that must be addressed to fully realize the promise of stochastic biological modelling. These canonical challengescan be summarized as follows: 
}%DIFDELCMD < \begin{enumerate} 
%DIFDELCMD < 	\item %%%
\DIFdel{\textbf{Intractable likelihoods:} }\DIFdelend The likelihood function \DIFdelbegin \DIFdel{for most agent-based models is }\DIFdelend \DIFaddbegin \DIFadd{is typically }\DIFaddend intractable, precluding \DIFdelbegin \DIFdel{the use of standard methods like }\DIFdelend \DIFaddbegin \DIFadd{standard statistical methods such as }\DIFaddend Maximum Likelihood Estimation (MLE) \DIFdelbegin \DIFdel{or typical }\DIFdelend \DIFaddbegin \DIFadd{and }\DIFaddend Markov chain Monte Carlo (MCMC)\DIFdelbegin \DIFdel{algorithms. }%DIFDELCMD < \item %%%
\DIFdel{\textbf{Surrogate-model error:} A common strategy to make inference tractable }\DIFdelend \DIFaddbegin \DIFadd{. Classical likelihood-free alternatives like Approximate Bayesian Computation (ABC) are computationally expensive and sensitive to tuning choices \citep{SunnkerABC,Sisson2018}. A common workaround }\DIFaddend is to replace the \DIFdelbegin \DIFdel{computationally expensive }\DIFdelend stochastic simulator with a \DIFdelbegin \DIFdel{cheap surrogate, typically a }\DIFdelend deterministic partial differential equation (PDE) \DIFdelbegin \DIFdel{describing mean-field behaviour. While this enables efficient MCMC or MLE techniques, the resulting inference is conditional on the surrogatebeing a faithful approximation, an assumption that, if violated, can lead to biased parameter estimates and inaccurate predictions. 
	}%DIFDELCMD < \item %%%
\DIFdel{\textbf{Noise-model misspecification:} The use of a deterministic surrogate creates the problem of specifying a noise model to connect its output to noisy data. A misspecified choice }\DIFdelend \DIFaddbegin \DIFadd{surrogate, but this introduces systematic bias and requires specifying an explicit noise model whose misspecification }\DIFaddend can lead to \DIFdelbegin \DIFdel{non-physical predictions and }\DIFdelend poorly calibrated uncertainties \DIFdelbegin \DIFdel{. 
	}%DIFDELCMD < \item %%%
\DIFdel{\textbf{Likelihood-free inefficiency:} Classical likelihood-free alternatives like Approximate Bayesian Computation (ABC) are notoriously computationally expensive and sensitive to the choice of summary statistics, distance metrics, and tolerance thresholds.
	}%DIFDELCMD < \item %%%
\DIFdel{\textbf{Information loss from summary statistics:} Classical methods often require reducing complex, high-dimensional data (e.g., the full 2D spatial arrangement of cells) to lower-dimensional summary statistics (e.g., a 1D cell density profile). This step risks a significant loss of information, and the choice of statistics can heavily bias the inference . 
	 }%DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \DIFadd{\citep{KennedyOHagan2001,BrynjarsdottirOHagan2014,SIMPSON2024111732}. These practical difficulties have motivated a growing interest in simulation-based inference methods that can learn directly from stochastic simulators \citep{Carr2021,SIMPSON2022111201,Simpson2025.05.25.656057,Ross2017ABCadhesion,Warne2022RapidBayes,Liu2024PRE}.
}\DIFaddend 

In this paper, we \DIFdelbegin \DIFdel{propose that these fundamental challenges can be addressed in a unified manner }\DIFdelend \DIFaddbegin \DIFadd{address these challenges }\DIFaddend using Neural Posterior Estimation (NPE), a \DIFdelbegin \DIFdel{modern }\DIFdelend simulation-based inference \DIFdelbegin \DIFdel{(SBI) paradigm. NPE leverages flexible neural density estimators to learn }\DIFdelend \DIFaddbegin \DIFadd{method that learns }\DIFaddend the posterior distribution \DIFdelbegin \DIFdel{$p\left(\theta \mid x\right)$ directly from simulator-generated $(\theta,x)$ pairs . This approach offers direct remedies to each of the aforementioned challenges: 
}%DIFDELCMD < \begin{enumerate} 
%DIFDELCMD < 	\item %%%
\DIFdel{It }\DIFdelend \DIFaddbegin \DIFadd{$p(\theta \mid x)$ directly from parameter-data pairs generated by the stochastic simulator \citep{papamakarios2016fast,greenberg2019automatic,Cranmer2020FrontierSBI}. NPE }\DIFaddend is likelihood-free, \DIFdelbegin \DIFdel{circumventing intractability.
	 }%DIFDELCMD < \item %%%
\DIFdel{It }\DIFdelend learns directly from the high-fidelity simulator \DIFdelbegin \DIFdel{, eliminating surrogate model bias and removing the PDE as a source of approximation error. 
	 }%DIFDELCMD < \item %%%
\DIFdel{It implicitly learns the full, complex, }\DIFdelend \DIFaddbegin \DIFadd{without surrogate approximations, and implicitly captures the }\DIFaddend stochastic relationship between parameters and data \DIFdelbegin \DIFdel{, obviating the need for }\DIFdelend \DIFaddbegin \DIFadd{without }\DIFaddend an explicit noise model. \DIFdelbegin %DIFDELCMD < \item %%%
\DIFdel{It }\DIFdelend \DIFaddbegin \DIFadd{NPE also }\DIFaddend provides amortized inference\DIFdelbegin \DIFdel{: after an initial, one-time training phase, it can generate posterior estimates for new observations almost instantaneously, overcoming the high per-inference cost of methods like ABC. }%DIFDELCMD < \item %%%
\DIFdel{It can ingest high-dimensional data directly (e.g., raw 2D images) }\DIFdelend , \DIFdelbegin \DIFdel{preserving information and avoiding manual selection of }\DIFdelend \DIFaddbegin \DIFadd{where the upfront cost of training is offset by near-instant posterior estimation for any new observation. We integrate a Convolutional Neural Network (CNN) as a learned feature extractor within the NPE pipeline, enabling inference directly from raw two-dimensional spatial data and reducing reliance on hand-crafted }\DIFaddend summary statistics.
\DIFdelbegin %DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdel{The central thesis of this work is that NPE provides a robust, efficient, and direct framework for parameter inference in stochastic biological models. }\DIFdelend \DIFaddbegin 

\DIFaddend We demonstrate this \DIFdelbegin \DIFdel{by applying a state-of-the-art Sequential NPE (SNPE) workflow to a canonical }\DIFdelend \DIFaddbegin \DIFadd{framework on a hierarchy of }\DIFaddend agent-based random walk \DIFdelbegin \DIFdel{model of cell migration and proliferation. This type of model is a well-established tool for studying collective cell behaviour \citep[see e.g.][]{Simpson2025.05.25.656057} and serves as an ideal test bed for advanced inference methods. 
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{From a modelling perspective, we perform inference directly on the high-fidelity, agent-based random walk model . This simulator-native approach entirely avoids the use of potentially unfaithful surrogate models (e.g., }\DIFdelend \DIFaddbegin \DIFadd{models of a barrier assay experiment, following the framework of \cite{Simpson2025.05.25.656057} (\SP{} hereafter) as our reference. Beginning with an isotropic baseline, we progressively add directional bias (chemotaxis), cell proliferation, and their combination. This hierarchy illustrates a progression from models where well-validated surrogate likelihoods exist and NPE is a convenient alternative to classical methods, to a combined model where the interplay of multiple mechanisms renders }\DIFaddend mean-field \DIFdelbegin \DIFdel{PDEs), thus eliminating surrogate-model error and preserving the full stochastic realism of the biological system. For inference , we deploy a modern Neural Posterior Estimation (NPE) framework as the core inferenceengine. This simulation-based, likelihood-free method directly learns the posterior distribution, overcoming the intractability of the likelihood function. Its amortized nature provides near-instantaneous posterior estimates for new observations after a single upfront training cost, bypassing the high per-inference expense and difficult tuning associated with traditional methods like ABC. A key methodological innovation is our integration of a deep learning architecture, specifically a Convolutional Neural Network (CNN), as a learned feature extractor for the NPE pipeline}\DIFdelend \DIFaddbegin \DIFadd{surrogate approximations increasingly unreliable and simulation-based inference provides a principled route to full Bayesian inference}\DIFaddend . \DIFdelbegin \DIFdel{This enables the inference framework to learn informative representations automatically and directly from raw , }\DIFdelend \DIFaddbegin \DIFadd{The remainder of this paper is organized as follows. Section~\ref{sec:methods} describes the random walk models, reviews classical inference approaches and their limitations, and details the NPE methodology and our CNN-based data processing pipeline. Section~\ref{sec:results} presents inference results across all four models using both one-dimensional summary statistics and raw }\DIFaddend two-dimensional spatial data, \DIFdelbegin \DIFdel{removing the need for manual, and potentially information-losing, data curation. We demonstrate how these components form a unified, end-to-end pipeline that represents a new paradigm for inference in spatially-explicit stochastic models. By coupling the CNN feature extractor with the NPE inference engine, we perform Bayesian parameter estimation directly using the raw spatial output of the agent-based model. This synergy bypasses two major traditional bottlenecks: the need for hand-crafted summary statistics and the reliance on approximate surrogate models. Additionally, we release an open-source, reproducible implementation of our pipeline to facilitate its adoption and extension to other complex, spatially-structured biological models. While we use scratch assay experiments as our primary motivating application, the inference framework developed here is applicable to the broader class of spatially-explicit stochastic models commonly used throughout biology and ecology. }\DIFdelend \DIFaddbegin \DIFadd{validates posteriors using simulation-based calibration diagnostics, and compares computational costs. Section~\ref{sec:discussion} discusses the implications of our findings for simulation-based inference in biology.
}\DIFaddend 



\DIFdelbegin \DIFdel{The remainder of this paper is structured as follows. Section \ref{sec:classical_inference} reviews the canonical stochastic model and establishes a performance baseline by applying classical inference methods. Section \ref{sec:NPE} details our NPE methodology and demonstrates its performance on one-dimensional summary statistics. Section \ref{sec:results_2d} further enhances this pipeline by embedding a Convolutional Neural Network (CNN) to perform inference directly on raw spatial data. Finally, Section \ref{sec:discussion} discusses the broader implications of our work for }\DIFdelend %DIF > % ============================================================
%DIF > %  2. METHODS
%DIF > % ============================================================
\DIFaddbegin \section{\DIFadd{Methods}}\label{sec:methods}

\subsection{\DIFadd{Random Walk Model}}\label{sec:model_description}

\begin{figure}[htbp]
	\centering
	\includegraphics[width=\textwidth]{images/pde_comparison_figure_column}
	\caption{\DIFaddFL{Schematic of the lattice-based random walk model. (a) Initial placement of agents within region $|x| \leq 25$ on a 2D lattice with $H=50$, $\Delta=\tau=1$, $U=0.3$. (b) Agent distribution after 100 time steps showing symmetric outward spreading. (c)--(d) Reduction of 2D data to 1D column counts $N_i(t)$ at $t=0$ and $t=100$ respectively. Blue dots show initial stochastic count data; orange dots show final stochastic count data; red curves show the continuum PDE solution $Hu(x,t)$ where $u(x,t)$ solves the diffusion equation with $D=P/4$.}}
	\label{fig:model_schematic}
\end{figure}

\DIFadd{We study a hierarchy of stochastic agent-based models of increasing complexity. Here we describe the baseline model; extensions incorporating directional bias, proliferation, and their combination are introduced progressively in Section~\ref{sec:results}. Agents representing biological cells are initially confined to a central region on a two-dimensional square lattice and subsequently spread outwards via an unbiased random walk. The experimental setup, illustrated in Figure~\ref{fig:model_schematic}, mimics a barrier assay where cells are placed in a confined region and allowed to spread after barrier removal. The two-dimensional lattice has height $H=50$ (with $0\leq y \leq 50$) and width $W=200$ (with $-100 \leq x \leq 100$), with lattice spacing $\Delta=1$ and discrete time step $\tau=1$. Zero-flux boundary conditions are applied at all edges. Initial conditions are set by occupying each site in the central region $|x| \leq 25$ with a single agent with probability $U=0.3$, as shown in Figure~\ref{fig:model_schematic}a. At each time step, the system evolves through a random sequential update method where agents move to one of four nearest-neighbour sites with probability $P=0.7$. After 100 time steps, the population spreads symmetrically as shown in Figure~\ref{fig:model_schematic}b.
}

\DIFadd{For lattice-based random walk models of this type, the mean behaviour of agents in the continuum limit can be described by a PDE of the form \citep{Codling2008,Simpson2025.05.25.656057,Plank2025}
}\begin{equation}
	\DIFadd{\frac{\partial u}{\partial t} = -\frac{\partial J}{\partial x} + S,
}\end{equation}
\DIFadd{where $u(x,t)$ represents mean agent density, $J$ is }\DIFaddend the \DIFdelbegin \DIFdel{future of simulation-based inference in biology}\DIFdelend \DIFaddbegin \DIFadd{macroscopic flux, and $S$ denotes source terms from proliferation. For the simple case considered here (unbiased motion, no proliferation, no crowding), this reduces to the linear diffusion equation
}\begin{equation}
	\DIFadd{\frac{\partial u}{\partial t} = D\frac{\partial^2 u}{\partial x^2},
}\end{equation}
\DIFadd{where $D = \lim_{\Delta,\tau \to 0} P\Delta^2/(4\tau)$ is the effective diffusion coefficient. Working with dimensionless units $\Delta=\tau=1$, this gives $D \approx P/4=0.175$ for our simulation with $P=0.7$}\DIFaddend .

\DIFdelbegin \section{\DIFdel{Classical Approaches to Inference in Stochastic Biological Models}}%DIFAUXCMD
\addtocounter{section}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{While \SP{} investigates a suite of model variations and alternative PDE approximations, we use this unbiased, non-interacting case as our baseline (the ``original model'') and progressively extend the analysis to three additional models. Model~A introduces directional bias via a parameter $\rho$ controlling the probability of stepping in the positive $x$-direction. Model~B incorporates cell proliferation through a Fisher--Kolmogorov mechanism with rate $R$. Model~C combines both bias and proliferation, representing the most biologically realistic scenario. We refer the reader to \SP{} for full model specifications and focus here on demonstrating how NPE handles the increasing complexity across this model hierarchy.
}



\subsection{\DIFadd{Classical Inference Approaches}}\DIFaddend \label{sec:classical_inference}
\DIFdelbegin \DIFdel{Before introducing our NPE framework (Section \ref{sec:NPE}), we }\DIFdelend \DIFaddbegin 

\DIFadd{We }\DIFaddend briefly review the established approaches to inference for stochastic agent-based models\DIFdelbegin \DIFdel{. We draw }\DIFdelend \DIFaddbegin \DIFadd{, drawing }\DIFaddend primarily on the \DIFdelbegin \DIFdel{comprehensive treatment in \cite{Simpson2025.05.25.656057} (}%DIFDELCMD < \SPhereafter%%%
\DIFdel{)}\DIFdelend \DIFaddbegin \DIFadd{treatment in \SP}\DIFaddend , which provides systematic comparisons across multiple inference paradigms for biological random-walk models. \DIFdelbegin \DIFdel{These methods have been the workhorses of parameter inference in mathematical biology, each with distinct strengths and limitations that motivate our proposed alternative. Table}\DIFdelend \DIFaddbegin \DIFadd{Table~}\DIFaddend \ref{tab:method_comparison} provides a comparative overview\DIFdelbegin \DIFdel{of these approaches}\DIFdelend .

\begin{table}[htbp]
	\centering
	\renewcommand{\arraystretch}{1.3} % Increase row spacing
	\begin{tabular}{@{}lcccc@{}}
		\toprule
		\textbf{Method} & \textbf{\DIFaddbeginFL \DIFaddFL{Per-observation }\DIFaddendFL Cost\DIFdelbeginFL \DIFdelFL{per Inference}\DIFdelendFL } & \textbf{Requires Surrogate?} & \textbf{Requires Summary Stats?} & \textbf{Amortized?\DIFaddbeginFL \DIFaddFL{\textsuperscript{\dag}}\DIFaddendFL } \\
		\midrule
		ABC & Very High & No & Yes & No \\
		Surrogate + MCMC & High & Yes & Yes & No \\
		Surrogate + MLE & Medium & Yes & Yes & No \\
		\textbf{NPE (proposed)} & \textbf{Low\textsuperscript{*}} & \textbf{No} & \textbf{Optional} & \textbf{Yes} \\
		\bottomrule
	\end{tabular}
	\vspace{0.2cm}
	\caption{Comparison of inference approaches for stochastic biological models. \textsuperscript{*}The low \DIFaddbeginFL \DIFaddFL{per-observation }\DIFaddendFL cost \DIFdelbeginFL \DIFdelFL{per inference }\DIFdelendFL for NPE is achieved after an initial, computationally intensive training phase. \DIFaddbeginFL \DIFaddFL{\textsuperscript{\dag}Amortized methods incur a one-time training cost after which per-observation cost is negligible.}\DIFaddendFL }
	\label{tab:method_comparison}
\end{table}

\DIFdelbegin \subsection{\DIFdel{Approximate Bayesian Computation}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \subsubsection{\DIFadd{Approximate Bayesian Computation}}
\DIFaddend 

\DIFdelbegin \DIFdel{Approximate Bayesian Computation (ABC )  }\DIFdelend \DIFaddbegin \DIFadd{ABC }\DIFaddend represents the most direct approach to likelihood-free inference for stochastic models \citep{SunnkerABC,Sisson2018,Tavare1997,Pritchard1999}. The foundational ABC rejection sampling algorithm approximates the posterior distribution by undertaking the following steps: (i) a parameter vector $\theta^{(j)}$ is drawn from the prior distribution $p(\theta)$; (ii) a dataset $Y^{(j)}$ is simulated from the model using $\theta^{(j)}$; (iii) a discrepancy between the summary statistics of the observed data, $s(Y^{\text{obs}})$, and simulated data, $s(Y^{(j)})$, is calculated using a distance metric $\rho(\cdot, \cdot)$; and (iv) the parameter $\theta^{(j)}$ is accepted if this discrepancy is less than a tolerance threshold, $\rho(s(Y^{\text{obs}}), s(Y^{(j)})) \le \epsilon$. The collection of accepted parameters forms a sample from an approximation to the true posterior, $p(\theta \mid Y^{\text{obs}})$ \citep{Beaumont2002}.


Despite its conceptual simplicity, ABC faces significant methodological and computational challenges. A primary issue is the choice of the tolerance $\epsilon$, which defines a fundamental bias-accuracy trade-off \citep{SunnkerABC,Blum2013}. Only in the idealised limit where $\epsilon \to 0$ and the summary statistics are sufficient does the ABC posterior converge to the true posterior \citep{Marin2012,Blum2013}. In practice, a smaller $\epsilon$ reduces bias but dramatically lowers the acceptance rate, increasing computational cost. Conversely, a larger $\epsilon$ increases the acceptance rate at the cost of yielding a more biased and diffuse posterior approximation.

This trade-off is exacerbated by the "curse of dimensionality" \citep{Blum2010,Blum2013}. For models with high-dimensional data or parameter spaces, the acceptance rate can \DIFdelbegin \DIFdel{be exceptionally low}\DIFdelend \DIFaddbegin \DIFadd{become vanishingly small}\DIFaddend , demanding a \DIFdelbegin \DIFdel{vast }\DIFdelend \DIFaddbegin \DIFadd{large }\DIFaddend number of simulations. For instance, \DIFdelbegin \DIFdel{S\&P2025 }\DIFdelend \DIFaddbegin \DIFadd{\SP{} }\DIFaddend generate $10^5$ simulations from their computationally expensive random walk model to retain just the top 1\%, highlighting this significant computational inefficiency. Furthermore, the method's performance depends critically on the choice of summary statistics. If the statistics are not formally sufficient, crucial information from the data is discarded, which introduces a form of bias that cannot be removed by simply lowering the tolerance $\epsilon$ \citep{Robert2011,Marin2012}. Selecting informative yet low-dimensional summary statistics in a principled manner remains a difficult and often ad-hoc challenge \citep{FearnheadPrangle2012,Blum2013}. More efficient variants\DIFdelbegin \DIFdel{of ABC}\DIFdelend , such as Sequential Monte Carlo ABC \citep{SissonFanTanaka2007,ToniWelchStrelkowaIpsenStumpf2009}, \DIFdelbegin \DIFdel{have been proposed to try to circumvent some of the above issues. }\DIFdelend \DIFaddbegin \DIFadd{iteratively refine a sequence of proposal distributions toward the posterior by progressively lowering the tolerance, improving the acceptance rate relative to simple rejection sampling. Nevertheless, the fundamental reliance on summary statistics and repeated simulation remains.
}\DIFaddend 



\DIFdelbegin \subsection{\DIFdel{Surrogate Model Approaches}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \subsubsection{\DIFadd{Surrogate Model Approaches}}
\DIFaddend To mitigate the computational burden of methods like ABC, an alternative strategy replaces the stochastic model with a computationally cheaper surrogate model. A common choice is a deterministic PDE that captures the system's mean-field behaviour, yielding a tractable likelihood function \citep{ErbanChapman2009,Baker2010} that enables standard, efficient likelihood-based inference algorithms like MCMC. However, this computational gain comes at the cost of introducing potential model misspecification bias, as inference is performed on an approximation of the true stochastic process.



The primary limitation of this approach is that the resulting inference is conditional on the fidelity of the surrogate. Any discrepancy between the surrogate and the true stochastic process introduces a systematic bias that cannot be reduced by collecting more data \citep{KennedyOHagan2001,BrynjarsdottirOHagan2014}. While the continuum limit accurately describes the mean behaviour of non-interacting random walks, the mean-field PDE neglects crucial stochastic effects in more complex scenarios. For example, in interacting models with crowding effects discussed in \DIFdelbegin \DIFdel{S\&P2025 }\DIFdelend \DIFaddbegin \DIFadd{\SP{} }\DIFaddend (Section 4), the mean-field approximation captures average behaviour but cannot represent the bounded nature of agent occupancy or the discrete stochastic fluctuations \citep{BrunaChapman2012,Baker2010} around this mean. This limitation becomes particularly important when constructing prediction intervals, where the choice of noise model (e.g. Gaussian vs. binomial, see \DIFdelbegin \DIFdel{next Section}\DIFdelend \DIFaddbegin \DIFadd{Section~}\DIFaddend \ref{sec:noise_model}) significantly affects the physical realism of predictions \citep{McCullaghNelder1989,Gelman1995}. These \DIFdelbegin \DIFdel{fundamental }\DIFdelend limitations of surrogate-based approaches motivate the need for inference methods that can work directly with the full stochastic simulator.



\subsubsection{Inference with Surrogate Likelihoods}
A tractable likelihood function from a surrogate model unlocks standard, efficient inference techniques that would be impossible with the original stochastic simulator. These techniques generally fall into two categories: direct optimization or posterior sampling.

The first category, direct optimization, seeks to find the single parameter set that maximizes the likelihood function, yielding the MLE. While computationally fast, the MLE provides only a point estimate and does not inherently quantify parameter uncertainty, although indirect (asymptotic) uncertainty quantification can be obtained through likelihood ratio tests and Wilks\DIFdelbegin \DIFdel{’ }\DIFdelend \DIFaddbegin \DIFadd{' }\DIFaddend theorem. The second category, posterior sampling, uses methods like MCMC to approximate the full posterior distribution, offering a complete characterisation of uncertainty. However, MCMC typically requires significant computational effort, careful hyper-parameter tuning, and rigorous convergence assessment. A third approach, the Laplace approximation, offers a compromise between the speed of optimization and the detailed uncertainty characterization of MCMC. This method approximates the posterior distribution as a multivariate normal distribution centred at the posterior mode (which equals the MLE, $\hat{\theta}$ under uniform priors). The covariance is given by the inverse of the observed Fisher Information matrix
\DIFdelbegin \DIFdel{:
}\DIFdelend \begin{equation}
	p(\theta \mid Y^{\text{obs}}) \approx \text{MVN}(\hat{\theta}, \mathcal{I}^{-1}(\hat{\theta})),
\end{equation}
where $\mathcal{I}(\hat{\theta}) = -\nabla\nabla^T \ell(\theta \mid Y^{\text{obs}})|_{\theta=\hat{\theta}}$ is the negative Hessian of the log-likelihood evaluated at the mode, often computed using automatic differentiation. While prediction intervals generated via the Laplace approximation can closely match those from more computationally intensive methods (see \SP), the method relies on the assumption that the posterior is locally Gaussian. This assumption may be violated for complex models, models with limited data, or simply for models with bounded parameters (e.g., $D \ge 0$; see Section\DIFdelbegin \DIFdel{\ref{sec:example_classical_results}}\DIFdelend \DIFaddbegin \DIFadd{~\ref{sec:results_classical}}\DIFaddend ) potentially leading to a poor representation of the true uncertainty.

However, all these likelihood-based approaches require an explicit specification of how the deterministic surrogate output relates to the noisy observational \DIFdelbegin \DIFdel{data—a }\DIFdelend \DIFaddbegin \DIFadd{data---a }\DIFaddend critical modelling choice we examine next.


\subsubsection{Noise Model Specification}\label{sec:noise_model}

The use of deterministic surrogates necessitates an explicit choice of how to model the relationship between the PDE output and the inherently noisy observations from the stochastic process. This choice profoundly impacts inference results, as the "noise" represents genuine stochastic variability in the agent-based system rather than measurement error.

\DIFdelbegin \DIFdel{S\&P2025 }\DIFdelend \DIFaddbegin \DIFadd{\SP{} }\DIFaddend systematically compare three noise models for relating observed counts to the PDE solution: Gaussian, Poisson, and binomial. The Gaussian noise model assumes observations are normally distributed around the model prediction with constant variance. While mathematically convenient, this leads to unphysical prediction intervals that include negative counts in low-density regions where the population density approaches zero, and fails to respect carrying capacity constraints in high-density regions. The
Poisson noise model provides a more natural choice for count data, eliminating the need for an additional variance parameter and ensuring non-negative predictions. The binomial noise model is appropriate for exclusion processes where occupancy is bounded, naturally capturing saturation effects.

\DIFdelbegin \DIFdel{S\&P2025}\DIFdelend \DIFaddbegin \DIFadd{\SP}\DIFaddend 's extensive comparisons reveal that noise model misspecification can significantly bias parameter estimates and lead to poorly calibrated prediction intervals. Quantitatively, the Gaussian model consistently produces unphysical negative predictions in low-density regions and encloses only 95\% of data points, while the Poisson and binomial models better respect the discrete, non-negative nature of count data and achieve coverage rates of 99-100\%. Similar conclusions regarding the superiority of binomial measurement models over standard additive Gaussian assumptions for biological count data have been demonstrated elsewhere \citep{SIMPSON2024111732}.



%DIF < \textbf{Gaussian noise:} The standard assumption that observations are normally distributed around the model prediction with constant variance $\sigma^2$:
%DIF < \begin{equation}
%DIF < 	N_i | \theta \sim \mathcal{N}(Hu(x_i, t), \sigma^2).
%DIF < \end{equation}
%DIF < While mathematically convenient, this leads to unphysical prediction intervals that include negative counts in low-density regions where $u(x,t) \to 0$.
\DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
%DIF < \textbf{Poisson noise:} For count data, a Poisson observation model provides a more natural choice:
%DIF < \begin{equation}
%DIF < 	N_i | \theta \sim \text{Pois}(Hu(x_i, t)),
%DIF < \end{equation}
%DIF < eliminating the additional variance parameter and ensuring non-negative predictions.
%DIFDELCMD < 

%DIFDELCMD < %%%
%DIF < \textbf{Binomial noise:} For exclusion processes where occupancy is bounded, the binomial model captures saturation effects:
%DIF < \begin{equation}
%DIF < 	N_i | \theta \sim \text{Binom}(H, u(x_i, t)).
%DIF < \end{equation}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \subsection{\DIFdelbegin \DIFdel{Example Results}\DIFdelend \DIFaddbegin \DIFadd{Neural Posterior Estimation}\DIFaddend }\DIFdelbegin %DIFDELCMD < \label{sec:example_classical_results}
%DIFDELCMD < \begin{figure}[htbp]
%DIFDELCMD < 	\centering
%DIFDELCMD < 	\includegraphics[width=\textwidth]{images/pde_comparison_figure_column}
%DIFDELCMD < 	%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Schematic of the lattice-based random walk model. (a) Initial placement of agents within region $|x| \leq 25$ on a 2D lattice with $H=50$, $\Delta=\tau=1$, $U=0.3$. (b) Agent distribution after 100 time steps showing symmetric outward spreading. (c)-(d) Reduction of 2D data to 1D column counts $N_i(t)$ at $t=0$ and $t=100$ respectively. Blue dots show initial stochastic count data; orange dots show final stochastic count data; red curves show the continuum PDE solution $Hu(x,t)$ where $u(x,t)$ solves the diffusion equation with $D=P/4$.}}
	%DIFAUXCMD
%DIFDELCMD < \label{fig:model_schematic}
%DIFDELCMD < \end{figure}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{To illustrate these theoretical considerations in practice, we reproduce a representative example from S\&P2025, which focuses on a stochastic agent-based model of a scratch barrier assay. In this model, agents representing biological cells are initially confined to a central region on a two-dimensional square lattice and subsequently spread outwards via an unbiased random walk. The experimental setup, illustrated in Figure~\ref{fig:model_schematic}, mimics a barrier assay where cells are initially placed in a confined region and allowed to spread after barrier removal. The two-dimensional lattice has height $H=50$ (with $0\leq y \leq 50$) and width $W=200$ (with $-100 \leq x \leq 100$), with lattice spacing $\Delta=1$ and discrete time step $\tau=1$. Zero-flux boundary conditions are applied at all edges. Initial conditions are set by occupying each site in the central region $|x| \leq 25$ with a single agent with probability $U=0.3$, as shown in Figure~\ref{fig:model_schematic}a. At each time step, the system evolves through a random sequential update method where agents move to one of four nearest-neighbour sites with probability $P=0.7$. After 100 time steps, the population spreads symmetrically as shown in Figure~\ref{fig:model_schematic}b.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{A key feature of this experimental design is the reduction of two-dimensional spatial data to one-dimensional measurements. Since the initial macroscopic density is uniform in the vertical ($y$) direction and boundary conditions preserve this symmetry, the resulting agent distribution remains independent of vertical location on average. This motivates quantifying the system by counting the number of agents per column:
}\begin{displaymath}
	\DIFdel{N_i(t) = \sum_{j=1}^{J} n_{i,j}(t),
}\end{displaymath}%DIFAUXCMD
\DIFdel{where $n_{i,j}(t)$ denotes the integer number of agents at lattice site $(i,j)$ at time $t$, and $J$ is the number of lattice sites in the vertical direction. This dimensional reduction from two -dimensional (2D) spatial data to one-dimensional (1D) column counts is standard in cell biology scratch assays \citep{Jin2016}, as it simplifies the characterization of spatio-temporal spreading while retaining the stochastic fluctuations inherent to the discrete process, as evident in Figure~\ref{fig:model_schematic}(c)-(d).
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{For lattice-based random walk models of this type, the mean behaviour of agents in the continuum limit can be described by a PDE of the form \citep{Codling2008,Simpson2025.05.25.656057,Plank2025}:
}\begin{displaymath}
	\DIFdel{\frac{\partial u}{\partial t} = -\frac{\partial J}{\partial x} + S,
}\end{displaymath}%DIFAUXCMD
\DIFdel{where $u(x,t)$ represents mean agent density, $J$ is the macroscopic flux, and $S$ denotes source terms from proliferation. For the simple case considered here (unbiased motion, no proliferation, no crowding), this reduces to the linear diffusion equation:
}\begin{displaymath}
	\DIFdel{\frac{\partial u}{\partial t} = D\frac{\partial^2 u}{\partial x^2},
}\end{displaymath}%DIFAUXCMD
\DIFdel{where $D = \lim_{\Delta,\tau \to 0} P\Delta^2/(4\tau)$ is the effective diffusion coefficient. Working with dimensionless units $\Delta=\tau=1$, this gives $D \approx P/4=0.175$ for our simulation with $P=0.7$.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{The continuous analogue of the column count $N_i$ under the PDE model is $Hu(x_i,t)$, where $x_i$ denotes the horizontal position of the $i$-th column and $H$ is the lattice height. Figure~\ref{fig:model_schematic}(c)-(d) compares the stochastic count data (blue dots) with this PDE solution (red curves), revealing that while the PDE captures mean trends, substantial stochastic fluctuations remain—a key motivation for carefully modelling observation noise.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{While S\&P2025 investigates a suite of model variations (e.g., incorporating proliferation, directional bias, and crowding effects) and alternative PDE approximations, we deliberately focus on this simple unbiased, non-interacting case. It provides a clear baseline against which to compare our proposed NPE method, and we refer the reader to the original study for a fuller discussion of more complex scenarios.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{We aim to infer two key parameters that govern the model's behaviour: the initial occupation probability, $U$, and the effective diffusion coefficient, $D$. For the synthetic data considered here, we use ground truth values $U=0.3$ and $D=0.175$. 
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Figure \ref{fig:sub1} shows the posterior obtained via ABC. This method operates directly on the stochastic model but relies on one-dimensional summary statistics (column counts), leading to a notably broad posterior. This diffuseness reflects both the inherent stochasticity of the system and the information loss from summarizing the data, showcasing the difficulty in precisely constraining parameters even with a high computational budget. In contrast, Figure \ref{fig:sub2} displays the Gaussian approximation to the posterior (Laplace approximation) derived from maximizing the likelihood of a deterministic surrogate. This approach is computationally efficient and yields a much tighter posterior. However, this apparent precision is entirely conditional on the fidelity of the surrogate; any mismatch between the surrogate and the true stochastic process introduces a systematic bias not reflected in the narrow posterior. Finally, Figure \ref{fig:sub3} shows the posterior sampled using MCMC with the surrogate model and an assumed Gaussian noise model. This provides a more flexible characterization of uncertainty. Crucially, this method requires inferring an additional nuisance parameter, $\sigma$, which quantifies the noise variance. Figure \ref{fig:sub3} shows the joint posterior over the model parameters and this additional noise term. While comprehensive, this result is still fundamentally limited by the choice of surrogate and the specified noise model. 
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Collectively, these results highlight the core dilemma: direct methods like ABC are computationally demanding and often imprecise, while surrogate-based methods are faster but introduce hard-to-quantify systematic biases that cannot be reduced by collecting more data.
}%DIFDELCMD < 

%DIFDELCMD < \begin{figure}[htbp]
%DIFDELCMD < 	\centering
%DIFDELCMD < 	\begin{subfigure}{0.45\textwidth}
%DIFDELCMD < 		\centering
%DIFDELCMD < 		\includegraphics[width=\textwidth]{images/corner_plot_abc_stochastic2}
%DIFDELCMD < 		%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Approximate posterior from ABC applied directly to the stochastic model. Computational constraints and information loss lead to a diffuse posterior for $D$.}}
		%DIFAUXCMD
%DIFDELCMD < \label{fig:sub1}
%DIFDELCMD < 	\end{subfigure}
%DIFDELCMD < 	\hfill
%DIFDELCMD < 	\begin{subfigure}{0.45\textwidth}
%DIFDELCMD < 		\centering
%DIFDELCMD < 		\includegraphics[width=\textwidth]{images/corner_plot_abc_surrogate2}
%DIFDELCMD < 		%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Gaussian approximation of the posterior (Laplace approximation) via MLE on a deterministic surrogate model. This approach is computationally efficient but conditional on the surrogate's fidelity.}}
		%DIFAUXCMD
%DIFDELCMD < \label{fig:sub2}
%DIFDELCMD < 	\end{subfigure}
%DIFDELCMD < 	

%DIFDELCMD < 	%%%
%DIF <  Add vertical space
	%DIFDELCMD < \vspace{1em}
%DIFDELCMD < 	

%DIFDELCMD < 	\begin{subfigure}{0.55\textwidth}
%DIFDELCMD < 		\centering
%DIFDELCMD < 		\includegraphics[width=\textwidth]{images/corner_plot_mcmc_surrogate2}
%DIFDELCMD < 		%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Posterior distribution from MCMC using the surrogate model with an assumed Gaussian noise model. This method jointly infers the model parameters alongside an additional noise variance parameter, $\sigma$.}}
		%DIFAUXCMD
%DIFDELCMD < \label{fig:sub3}
%DIFDELCMD < 	\end{subfigure}
%DIFDELCMD < 	%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Comparison of posterior distributions obtained using classical inference methods for a scratch assay model. The diagonal panels show the marginalized one-dimensional posteriors for each parameter, while the off-diagonal panel shows the joint two-dimensional posterior illustrating their covariance. The subtitles above each one-dimensional subplot indicate the  posterior median and the central 68\% credible interval. The vertical dashed lines mark 16th, 50th, and 84th percentiles of each one-dimensional posterior, corresponding to the median (central line) and the bounds of the central 68\% credible interval. The orange lines indicate the true parameter values. Each approach demonstrates distinct trade-offs between computational efficiency, precision, and model assumptions.}}
	%DIFAUXCMD
%DIFDELCMD < \label{fig:three_figures}
%DIFDELCMD < \end{figure}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{These persistent challenges across classical approaches motivate our exploration of NPE as a unified framework that can address multiple limitations simultaneously through direct learning from simulator output.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\section{\DIFdel{Neural Posterior Estimation for Stochastic Biological Models}}%DIFAUXCMD
\addtocounter{section}{-1}%DIFAUXCMD
\DIFdelend \label{sec:NPE}

\DIFdelbegin \subsection{\DIFdel{Overview and Motivation}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdel{NPE \citep{papamakarios2016fast, greenberg2019automatic,Cranmer2020FrontierSBI} offers a fundamentally different }\DIFdelend \DIFaddbegin \subsubsection{\DIFadd{Overview}}
\DIFadd{NPE \citep{papamakarios2016fast, greenberg2019automatic,Cranmer2020FrontierSBI} takes a distinct }\DIFaddend approach to inference for simulator-based models \DIFaddbegin \DIFadd{within the broader simulation-based inference framework}\DIFaddend . Rather than relying on surrogate approximations or requiring explicit noise models, NPE learns the posterior distribution $p(\theta | Y)$ directly from simulations of the stochastic model. This is achieved through conditional density estimation using neural networks\DIFdelbegin \DIFdel{, providing a unified solution to the challenges identified in Section \ref{sec:classical_inference}}\DIFdelend .

\DIFdelbegin \DIFdel{The key insight is that while }\DIFdelend \DIFaddbegin \DIFadd{While }\DIFaddend the likelihood $p(Y|\theta)$ may be intractable for complex stochastic simulators, we can still readily generate samples from it by running the simulator. NPE leverages these (parameter, sample) pairs to train a neural network that approximates the inverse \DIFdelbegin \DIFdel{mapping — from }\DIFdelend \DIFaddbegin \DIFadd{mapping---from }\DIFaddend an observation back to the distribution over parameters that could have produced it. This process is \DIFdelbegin \DIFdel{\textit{amortized}; }\DIFdelend \DIFaddbegin \DIFadd{amortized: }\DIFaddend once the network is trained, it can perform posterior inference for any new observation in seconds rather than hours, without requiring additional simulations. For the class of stochastic biological models considered here, this approach directly addresses each limitation of classical methods: it circumvents intractable likelihoods, eliminates surrogate model bias, implicitly captures complex stochastic relationships without explicit noise models, and can process high-dimensional data through automatic feature extraction.

Figure\DIFaddbegin \DIFadd{~}\DIFaddend \ref{fig:npe_workflow} provides a schematic overview of the NPE workflow, illustrating the distinction between the computationally intensive training phase and the rapid inference phase. During training, parameters are sampled from the prior $p(\theta)$ and used to generate simulated data via the stochastic simulator $p(Y|\theta)$. These parameter-data pairs $\{(\theta_i, Y_i)\}_{i=1}^N$ are then used to train a neural density estimator $q_\phi(\theta|Y)$ that learns to approximate the posterior distribution. Once trained, the inference phase is nearly instantaneous: given new observed data $Y_{\rm obs}$, the network directly evaluates the approximate posterior $p(\theta|Y_{\rm obs})$ without requiring additional simulations.

\begin{figure}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \includegraphics[width=0.95\textwidth]{images/flow_diagram}
%DIFDELCMD < 	%%%
\DIFdelendFL \DIFaddbeginFL \includegraphics[width=0.95\textwidth]{images/npe_workflow-2}
	\DIFaddendFL \caption{Schematic overview of the NPE workflow. The training phase (left) involves sampling parameters from the prior, generating simulated data, and training a neural density estimator on the resulting parameter-data pairs. The inference phase (right) uses the trained network to instantly evaluate the posterior for new observations without additional simulations.}
	\label{fig:npe_workflow}
\end{figure}

\DIFdelbegin \subsection{\DIFdel{Mathematical Foundation}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \subsubsection{Conditional Normalizing Flows}

At the core of NPE are normalizing flows \DIFaddbegin \DIFadd{\cite{rezende2015variational, papamakarios2021normalizing}}\DIFaddend , a class of \DIFdelbegin \DIFdel{flexible neural architectures that learn }\DIFdelend \DIFaddbegin \DIFadd{generative models that construct }\DIFaddend complex probability distributions \DIFdelbegin \DIFdel{through invertible transformations. Normalizing flows have emerged as a powerful tool for density estimation in high-dimensional spaces \cite{rezende2015variational, papamakarios2021normalizing}, offering significant advantages over traditional methods like mixture models or kernel density estimation when dealing with complex, multimodal distributions typical in biological inference problems. The fundamental idea behind normalizing flows is deceptively simple: construct a complex target distribution }\DIFdelend by transforming samples from a simple base distribution (typically a standard Gaussian) through a series of invertible, differentiable mappings \DIFdelbegin \DIFdel{. This approach can be understood as a sophisticated generalization of the familiar inverse transform sampling method,  extended to high dimensions through neural networks \cite{kobyzev2020normalizing}. Where traditional }\DIFdelend \DIFaddbegin \DIFadd{\cite{kobyzev2020normalizing}. Because each mapping is invertible and has a tractable Jacobian, the resulting density can be evaluated exactly via the }\DIFaddend change-of-variables \DIFdelbegin \DIFdel{techniques might struggle with complex distributions, normalizing flows leverage the representational power of deep learning while maintaining the mathematical rigour needed for proper density estimation }\DIFdelend \DIFaddbegin \DIFadd{formula, making normalizing flows well suited to the conditional density estimation task required by NPE}\DIFaddend .


Formally, let $u \in \mathbb{R}^d$ be a random variable with base density $p_u(u) = \mathcal{N}(0, I)$, where $I \in \mathbb{R}^{d \times d}$ denotes the identity matrix. The flow defines an invertible transformation $f_\phi: \mathbb{R}^d \rightarrow \mathbb{R}^d$ parametrized by neural network weights $\phi$. For conditional density estimation, this transformation is conditioned on the observation: \DIFdelbegin \DIFdel{$\theta = f_\phi(u; x)$}\DIFdelend \DIFaddbegin \DIFadd{$\theta = f_\phi(u; Y)$}\DIFaddend . The invertibility requirement ensures that we can both generate samples (forward pass) and evaluate densities (inverse pass). \DIFdelbegin \DIFdel{The key insight is that complex }\DIFdelend \DIFaddbegin \DIFadd{Complex }\DIFaddend distributions can be learned by composing simple transformations, each of which maintains invertibility and efficient Jacobian computation. This allows the network to learn highly flexible posterior shapes while maintaining computational tractability, a crucial feature when inferring parameters in complex biological models where posteriors may exhibit non-Gaussian features such as multimodality, heavy tails, or curved degeneracies. The resulting approximate posterior density follows from the change of variables formula
\DIFdelbegin \DIFdel{:
}\DIFdelend \begin{equation}
	q_\phi(\theta | Y) = p_u(f_\phi^{-1}(\theta;Y)) \left| \det J_{f_\phi^{-1}}(\theta; Y) \right|
\end{equation}
where $J_{f_\phi^{-1}}$ denotes the Jacobian of the inverse transformation. Intuitively, the determinant of the Jacobian corrects for the local change in volume caused by the transformation, ensuring the resulting density is properly normalized. To understand this geometrically: when the transformation $f_{\phi}$
stretches space in some region, the determinant accounts for this stretching to ensure probability mass is conserved \cite{papamakarios2021normalizing}. Modern flow architectures (e.g., coupling flows \cite{dinh2016density}, autoregressive flows \cite{papamakarios2017masked}) are designed such that this determinant is computationally efficient to \DIFdelbegin \DIFdel{calculate—often }\DIFdelend \DIFaddbegin \DIFadd{calculate---often }\DIFaddend reducing to a summation or product of scalar terms rather than requiring explicit $d \times d$ matrix determinant computation. For readers familiar with traditional MCMC-based inference in biological models, normalizing flows offer a complementary approach: rather than exploring the posterior through iterative sampling, they learn an explicit parametric approximation that enables fast posterior evaluation and sampling once trained \cite{Cranmer2020FrontierSBI}. This \DIFdelbegin \DIFdel{amortization — training }\DIFdelend \DIFaddbegin \DIFadd{amortization---training }\DIFaddend once on simulated data, then instantly obtaining posteriors for new \DIFdelbegin \DIFdel{observations — makes }\DIFdelend \DIFaddbegin \DIFadd{observations---makes }\DIFaddend them particularly attractive for scenarios requiring repeated inference, such as model selection, experimental design, or real-time data analysis.




\subsubsection{Training Procedure}

NPE training requires a dataset of parameter-data pairs generated from the simulator. The training procedure follows three main steps:

\begin{enumerate}
	\item \textbf{Simulation:} Generate $N$ training samples by drawing parameters from the prior $\theta_i \sim p(\theta)$ and simulating corresponding data $Y_i \sim p(Y|\theta_i)$ using the stochastic model (left section, Figure \ref{fig:npe_workflow}).

	\item \textbf{Optimization:} Train the normalizing flow by maximizing the log-likelihood of parameters given their corresponding simulated data:
	\begin{equation}
		\mathcal{L}(\phi) = -\frac{1}{N} \sum_{i=1}^N \log q_\phi(\theta_i | Y_i) \, .
	\end{equation}
This objective is optimized using stochastic gradient descent (e.g., Adam \citep{kingma2014adam}) with mini-batches of simulated pairs $(\theta_i, Y_i)$. During training, gradients are computed via back-propagation through the flow transformation $f_\phi(u; x)$, updating the network parameters $\phi$ until convergence. This learns the transformation that maps from a simple base distribution to the posterior distribution conditioned on the data (middle section, Figure \ref{fig:npe_workflow}).
	\item \textbf{Inference:} For observed data $Y_{\rm obs}$, obtain the approximate posterior $q_\phi(\theta | Y_{\rm obs})$ by evaluating the trained network. Generate posterior samples by drawing $u \sim p_u(u)$ and applying the learned transformation $\theta = f_\phi(u; Y_{\rm obs})$ (right section, Figure \ref{fig:npe_workflow}).
\end{enumerate}
The training data requirements scale with problem complexity, but it is difficult in general to provide a quantitative recommendation on the number of required simulations to train the model. Typically at least $N = 10^4 - 10^6$ simulations suffice for moderate-dimensional parameter spaces ($d \leq 10$), though higher-dimensional problems may require larger datasets \citep{2025arXiv250812939D}. Crucially, this procedure is \DIFdelbegin \DIFdel{\textit{amortized}}\DIFdelend \DIFaddbegin \DIFadd{amortized}\DIFaddend ; the computationally expensive training phase is performed only once, after which posterior inference for any new observation is nearly instantaneous. This represents a significant advantage over methods like ABC, which require fresh simulations for each new dataset. We provide quantitative comparisons in Section\DIFaddbegin \DIFadd{~}\DIFaddend \ref{sec:runtime}.

\DIFdelbegin \subsection{\DIFdel{Sequential Neural Posterior Estimation}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend %DIF >  --- SNPE subsection commented out: not used in current results ---
%DIF >  \subsubsection{Sequential Neural Posterior Estimation}
%DIF > 
%DIF >  While the basic NPE algorithm is powerful, it can be inefficient when the prior is much broader than the posterior. Sequential Neural Posterior Estimation (SNPE) \citep{papamakarios2016fast, greenberg2019automatic} addresses this through an iterative refinement process that concentrates computational effort on parameter regions consistent with the observed data.
%DIF > 
%DIF >  The sequential algorithm proceeds as follows:
%DIF > 
%DIF >  \begin{enumerate}
%DIF >  	\item \textbf{Round 1:} Train an initial density estimator using simulations from the prior, obtaining $q_{\phi_1}(\theta | x)$.
%DIF > 
%DIF >  	\item \textbf{Subsequent rounds $(r > 1)$:} Use the posterior approximation from round $r-1$ evaluated at the observed data, $q_{\phi_{r-1}}(\theta | x_o)$, as a proposal distribution. Generate new simulations $\theta \sim q_{\phi_{r-1}}(\theta | x_o)$, $x \sim p(x|\theta)$.
%DIF > 
%DIF >  	\item \textbf{Importance weighting correction:} Train a new density estimator $q_{\phi_r}(\theta | x)$ on the focused simulations, with importance weighting to correct for the proposal distribution bias
%DIF >  	\begin{equation}
%DIF >  		\mathcal{L}_r(\phi) = -\frac{1}{N_r} \sum_{i=1}^{N_r} w_i \log q_\phi(\theta_i | x_i)
%DIF >  	\end{equation}
%DIF >  	where $w_i = p(\theta_i) / q_{\phi_{r-1}}(\theta_i | x_o)$ corrects for sampling from the proposal rather than the prior.
%DIF >  \end{enumerate}
%DIF > 
%DIF >  SNPE is most beneficial when the prior is substantially broader than the posterior, which is common in biological applications where parameter ranges are often set conservatively. The method typically converges within 2--4 rounds, providing substantial efficiency gains over standard NPE while maintaining accuracy. In this work we employ single-round NPE, training the density estimator on a large simulation budget drawn from the prior. While SNPE can improve sample efficiency by iteratively focusing simulations, our prior ranges are sufficiently constrained that single-round training provides accurate posteriors without sequential refinement.
%DIF >  --- End commented-out SNPE subsection ---

\DIFdelbegin \DIFdel{While the basic NPE algorithm is powerful, it can be inefficient }\DIFdelend \DIFaddbegin \subsubsection{\DIFadd{Implementation Details}}

\DIFadd{All NPE analyses in this work use the \texttt{sbi} Python package \cite{Boelts2025sbi}, which provides a well-documented implementation of state-of-the-art simulation-based inference algorithms. We employ neural spline flows \cite{durkan2019neural} as our density estimator architecture. Neural spline flows construct invertible transformations from monotonic rational-quadratic spline segments, providing highly expressive density estimation with analytically tractable Jacobians and stable training dynamics. We use single-round NPE throughout; sequential variants \citep{papamakarios2016fast, greenberg2019automatic} can improve sample efficiency }\DIFaddend when the prior is much broader than the posterior\DIFdelbegin \DIFdel{. Sequential Neural Posterior Estimation (SNPE) \citep{papamakarios2016fast, greenberg2019automatic} addresses this through an iterative refinement process that concentrates computational effort on parameter regions consistent with the observed data}\DIFdelend \DIFaddbegin \DIFadd{, but our prior ranges are sufficiently constrained that single-round training provides accurate posteriors without sequential refinement. Additional implementation details can be found in the project GitHub repository }\footnote{\href{github.com/tomkimpson/NPE_for_RW_inference}{\DIFadd{https://github.com/tomkimpson/NPE\_for\_RW\_inference}}}\DIFaddend .

\DIFdelbegin \DIFdel{The sequential algorithm proceeds as follows:
}\DIFdelend \DIFaddbegin \DIFadd{Prior distributions for each model are specified alongside the corresponding inference results in Section~\ref{sec:results}. In all cases we use broad uniform priors determined by the physical constraints of the model parameters; for NPE, the prior serves the dual role of defining the training distribution from which simulation parameters are drawn and entering into the posterior distribution via Bayes' theorem \citep{Gelman1995,Gelman2017}.
}\DIFaddend 



\DIFdelbegin %DIFDELCMD < \begin{enumerate}
%DIFDELCMD < 	\item %%%
\DIFdel{\textbf{Round 1:} Train an initial density estimator using simulations from the prior, obtaining $q_{\phi_1}(\theta | x)$. }\DIFdelend \DIFaddbegin \subsection{\DIFadd{Data Representations and CNN Architecture}}\label{sec:data_architecture}
\DIFaddend 

\DIFdelbegin %DIFDELCMD < \item %%%
\DIFdel{\textbf{Subsequent rounds $(r > 1)$:} Use the posterior approximation from round $r-1$ evaluated at the observed data , $q_{\phi_{r-1}}(\theta | x_o)$, as a proposal distribution. Generate new simulations $\theta \sim q_{\phi_{r-1}}(\theta | x_o)$, $x \sim p(x|\theta)$. }%DIFDELCMD < 

%DIFDELCMD < 	\item %%%
\DIFdel{\textbf{Importance weighting correction:} Train a new density estimator $q_{\phi_r}(\theta | x)$ on the focused simulations, with importance weighting to correct for the proposal distributionbias:
	}\begin{displaymath}
		\DIFdel{\mathcal{L}_r(\phi) = -\frac{1}{N_r} \sum_{i=1}^{N_r} w_i \log q_\phi(\theta_i | x_i)
	}\end{displaymath}%DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{The barrier assay experimental design naturally lends itself to a dimensional reduction from two-dimensional spatial data to one-dimensional measurements. Since the initial macroscopic density is uniform in the vertical ($y$) direction and boundary conditions preserve this symmetry, the resulting agent distribution remains independent of vertical location on average. This motivates quantifying the system by counting the number of agents per column
}\begin{equation}
	\DIFadd{N_i(t) = \sum_{j=1}^{H} n_{i,j}(t),
}\end{equation}\DIFaddend 
where \DIFdelbegin \DIFdel{$w_i = p(\theta_i) / q_{\phi_{r-1}}(\theta_i | x_o)$ corrects for sampling from the proposal rather than the prior.
}%DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \DIFadd{$n_{i,j}(t)$ denotes the integer number of agents at lattice site $(i,j)$ at time $t$, and $H$ is the number of lattice sites in the vertical direction. This dimensional reduction from 2D spatial data to 1D column counts is standard in cell biology scratch assays \citep{Jin2016ScratchRepro}, as it simplifies the characterization of spatio-temporal spreading while retaining the stochastic fluctuations inherent to the discrete process, as evident in Figure~\ref{fig:model_schematic}(c)--(d). The continuous analogue of the column count $N_i$ under the PDE model is $Hu(x_i,t)$, where $x_i$ denotes the horizontal position of the $i$-th column and $H$ is the lattice height. Figure~\ref{fig:model_schematic}(c)--(d) compares the stochastic count data (blue dots) with this PDE solution (red curves), revealing that while the PDE captures mean trends, substantial stochastic fluctuations remain.
}\DIFaddend 

\DIFdelbegin \DIFdel{SNPE is most beneficial when the prior is substantially broader than the posterior,which is common in biological applications where parameter ranges are often set conservatively. The method typically converges within 2-4 rounds, providing substantial efficiency gains over standard NPE while maintaining accuracy. In this work we will exclusively use the sequential version of NPE. }\DIFdelend \DIFaddbegin \DIFadd{While column counts are the standard data representation for barrier assays, this dimensional reduction necessarily discards spatial information---such as clustering patterns, local density fluctuations, and correlations in the vertical direction---that may help constrain parameters in models where processes like proliferation generate spatial structure not captured by one-dimensional projections (see Section~\ref{sec:results_2d}). We therefore also develop a pipeline that operates directly on the full two-dimensional spatial data. We treat each lattice configuration from the simulator as a 2D histogram (or count-based image), where each pixel's value represents the integer number of agents $n_{i,j}(t)$ at the corresponding lattice site. To learn informative features directly from this high-dimensional input, we replace the simple feed-forward network with a CNN \citep{lecun1998,krizhevsky2012,goodfellow2016}. The CNN acts as a learnable feature extractor embedded within the NPE pipeline. It uses a series of convolutional and pooling layers to progressively downsample the spatial data into a low-dimensional embedding. This process captures salient spatial features, such as clustering patterns and local agent densities, while preserving translational invariance. The learned embedding is then passed to the remainder of the network to estimate the posterior distribution. This end-to-end training ensures that the features extracted by the CNN are optimized for parameter inference.
}\DIFaddend 

\DIFdelbegin \subsection{\DIFdel{Implementation Details}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{Table~\ref{tab:hyperparameters} summarises the key hyperparameters for both the 1D and 2D NPE configurations. A detailed specification of the CNN architecture and full implementation details can be found in the project GitHub repository.
}\DIFaddend 

\DIFdelbegin \DIFdel{All NPE analyses in this work use the \texttt{sbi} Python package \cite{2020JOSS....5.2505T}, which provides a well-documented implementation of state-of-the-art }\DIFdelend \DIFaddbegin \begin{table}[htbp]
	\centering
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{@{}lcc@{}}
		\toprule
		\DIFaddFL{\textbf{Hyperparameter} }& \DIFaddFL{\textbf{1D (Column Counts)} }& \DIFaddFL{\textbf{2D (Spatial / CNN)} }\\
		\midrule
		\DIFaddFL{Input dimension }& \DIFaddFL{200 }& \DIFaddFL{$50 \times 200 = 10{,}000$ }\\
		\DIFaddFL{Density estimator }& \DIFaddFL{Neural Spline Flows }& \DIFaddFL{Neural Spline Flows }\\
		\DIFaddFL{Coupling layers }& \DIFaddFL{8 }& \DIFaddFL{8 }\\
		\DIFaddFL{Hidden features }& \DIFaddFL{256 }& \DIFaddFL{256 }\\
		\DIFaddFL{Embedding network }& \DIFaddFL{Feed-forward }& \DIFaddFL{CNN (4 conv.\ stages) }\\
		\DIFaddFL{Optimizer }& \DIFaddFL{Adam }& \DIFaddFL{Adam }\\
		\DIFaddFL{Learning rate }& \DIFaddFL{$10^{-4}$ }& \DIFaddFL{$10^{-4}$ }\\
		\DIFaddFL{Batch size }& \DIFaddFL{512 }& \DIFaddFL{512 }\\
		\DIFaddFL{Max epochs }& \DIFaddFL{100 }& \DIFaddFL{100 }\\
		\DIFaddFL{Early stopping patience }& \DIFaddFL{20 epochs }& \DIFaddFL{20 epochs }\\
		\DIFaddFL{Validation fraction }& \DIFaddFL{0.1 }& \DIFaddFL{0.1 }\\
		\DIFaddFL{Training simulations }& \DIFaddFL{50}{\DIFaddFL{,}}\DIFaddFL{000 }& \DIFaddFL{50}{\DIFaddFL{,}}\DIFaddFL{000 }\\
		\bottomrule
	\end{tabular}
	\caption{\DIFaddFL{Summary of key hyperparameters for the single-round NPE pipeline in the 1D and 2D configurations. All models were trained on an NVIDIA A100-SXM4-80GB GPU using the \texttt{sbi} library \citep{Boelts2025sbi} with a PyTorch backend.}}
	\label{tab:hyperparameters}
\end{table}



%DIF > % ============================================================
%DIF > %  3. RESULTS
%DIF > % ============================================================
\section{\DIFadd{Results}}\label{sec:results}

\DIFadd{We present inference results for four progressively complex random walk models. We begin with the original isotropic model (Section~\ref{sec:original_model}), which serves as a baseline comparison with classical methods. We then extend the analysis to models incorporating directional bias (Section~\ref{sec:model_a}), proliferation (Section~\ref{sec:model_b}), and their combination (Section~\ref{sec:model_c}). For each model, we report NPE posteriors from both 1D column count and 2D spatial data. Section~\ref{sec:diagnostics} validates all posteriors using }\DIFaddend simulation-based \DIFdelbegin \DIFdel{inference algorithms. We employ neural spline flows \cite{durkan2019neural} as our density estimator architecture, which offer excellent expressivity while maintaining computational efficiency. Additional implementation details can be found in the project GitHub repository }\footnote{\href{github.com/tomkimpson/NPE_for_RW_inference}{\DIFdel{https://github.com/tomkimpson/NPE\_for\_RW\_inference}}%DIFAUXCMD
}%DIFAUXCMD
\addtocounter{footnote}{-1}%DIFAUXCMD
\DIFdel{. }\DIFdelend \DIFaddbegin \DIFadd{calibration diagnostics and Section~\ref{sec:runtime} compares computational costs. Reproducibility across random seeds is examined in Appendix~\ref{sec:reproducibility}.
}\DIFaddend 

\DIFdelbegin \subsection{\DIFdel{Application to Barrier Assay Data: 1D Column Count Analysis}} %DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \subsection{\DIFadd{Original Model}}\label{sec:original_model}

\DIFadd{For the original isotropic model, we infer two parameters: the initial occupation probability $U$ (cell seeding density) and the motility probability $P$, where the effective diffusion coefficient is $D = P/4$. We use ground truth values $U=0.3$ and $D=0.175$.
}

\subsubsection{\DIFadd{Classical Inference Baseline}}\label{sec:results_classical}

\DIFadd{Figure~\ref{fig:sub1} shows the posterior obtained via ABC. This method operates directly on the stochastic model but relies on one-dimensional summary statistics (column counts), leading to a notably broad posterior. This diffuseness reflects both the inherent stochasticity of the system and the information loss from summarizing the data, showcasing the difficulty in precisely constraining parameters even with a high computational budget. In contrast, Figure~\ref{fig:sub2} displays the Gaussian approximation to the posterior (Laplace approximation) derived from maximizing the likelihood of a deterministic surrogate. This approach is computationally efficient and yields a much tighter posterior. However, this apparent precision is entirely conditional on the fidelity of the surrogate; any mismatch between the surrogate and the true stochastic process introduces a systematic bias not reflected in the narrow posterior. Finally, Figure~\ref{fig:sub3} shows the posterior sampled using MCMC with the surrogate model and an assumed Gaussian noise model. This provides a more flexible characterization of uncertainty. Crucially, this method requires inferring an additional nuisance parameter, $\sigma$, which quantifies the noise variance. Figure~\ref{fig:sub3} shows the joint posterior over the model parameters and this additional noise term. While comprehensive, this result is still limited by the choice of surrogate and the specified noise model.
}

\DIFadd{Collectively, these results highlight the central trade-off: direct methods like ABC are computationally demanding and often imprecise, while surrogate-based methods are faster but introduce hard-to-quantify systematic biases that cannot be reduced by collecting more data.
}

\begin{figure}[htbp]
	\centering
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_plot_abc_stochastic2}
		\caption{}
		\label{fig:sub1}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_plot_abc_surrogate2}
		\caption{}
		\label{fig:sub2}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_plot_mcmc_surrogate2}
		\caption{}
		\label{fig:sub3}
	\end{subfigure}
	\caption{\DIFaddFL{Comparison of posterior distributions obtained using classical inference methods for a scratch assay model. (a)~Approximate posterior from ABC applied directly to the stochastic model; computational constraints and information loss lead to a diffuse posterior for $D$. (b)~Gaussian approximation of the posterior (Laplace approximation) via MLE on a deterministic surrogate model; this approach is computationally efficient but conditional on the surrogate's fidelity. (c)~Posterior distribution from MCMC using the surrogate model with an assumed Gaussian noise model; this method jointly infers the model parameters alongside an additional noise variance parameter, $\sigma$. In all panels, the diagonal shows the marginalized one-dimensional posteriors for each parameter and the off-diagonal shows the joint two-dimensional posterior. The subtitles above each one-dimensional subplot indicate the posterior median and the central 68\% credible interval. The vertical dashed lines mark the 16th, 50th, and 84th percentiles, and the orange lines indicate the true parameter values. No true value is shown for $\sigma$ in panel~(c) because it is a nuisance parameter introduced by the surrogate modelling approach; the true data-generating process is the stochastic simulator, which has no corresponding noise variance parameter.}}
	\label{fig:three_figures}
\end{figure}



\subsubsection{\DIFadd{NPE with 1D Summary Statistics}}\DIFaddend \label{sec:1d_results}

We demonstrate NPE's effectiveness by applying it to the same synthetic barrier assay data used in Section\DIFdelbegin \DIFdel{\ref{sec:example_classical_results}}\DIFdelend \DIFaddbegin \DIFadd{~\ref{sec:results_classical}}\DIFaddend . To ensure direct comparison with the classical methods, we first implement NPE using identical 1D column count summary statistics $N_i$, representing the number of agents in each vertical column of the lattice. \DIFdelbegin \DIFdel{In Section \ref{sec:results_2d} we will extend this to operate directly on the 2D spatial data.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\subsubsection{\DIFdel{Experimental Setup}}
%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
\DIFdelend Following the protocol of \SP, we generate synthetic observations from a stochastic random walk model with true parameters $P = 0.7$ and $U = 0.3$, corresponding to diffusivity $D \approx P/4 = 0.175$. The resulting data consists of a 201-dimensional vector of column counts, corresponding to the 201 discrete lattice columns from $x=-100$ to $x=+100$. We perform inference directly on the stochastic model parameters $P$ and $U$, then transform to $D$ via the relationship $D = P/4$ for comparison with PDE-based approaches. \DIFdelbegin \DIFdel{We use  broad uniform priors covering the full parameter space: $P \sim \mathcal{U}(0.0, 1.0)$ and $U \sim \mathcal{U}(0.01, 1.0)$. These priors are determined by the physical constraints of the model: $U$ and $P$ are both probabilities and so must lie in $[0,1]$. The lower bound of 0.01 for $U$ excludes the degenerate case of zero initial agents while remaining sufficiently permissive. In the absence of strong prior information about parameter values, these uniform priors represent a minimally informative choice that allows the data to dominate the posterior inference. For NPE , the prior serves the dual role of defining the training distribution from which simulation parameters are drawn, and entering into the posterior distribution via Bayes' theorem \citep{Gelman1995,Gelman2017}
}%DIFDELCMD < 

%DIFDELCMD < %%%
\subsubsection{\DIFdel{Parameter Inference Results}}
%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
\DIFdel{Figure \ref{fig:npe_1d_posterior} presents the posterior distribution obtained via NPE. NPE }\DIFdelend \DIFaddbegin \DIFadd{NPE }\DIFaddend recovers a posterior distribution with median \DIFdelbegin \DIFdel{$U = 0.298^{+0.009}_{-0.008}$ and $D=0.164^{+0.024}_{-0.024}$}\DIFdelend \DIFaddbegin \DIFadd{$U = 0.293 \pm 0.008$ and $D=0.162^{+0.027}_{-0.021}$ (Figure~\ref{fig:npe_original_1d})}\DIFaddend . The results demonstrate excellent agreement with the true parameter values ($U = 0.3$, $D = 0.175$).

This approach yields several key advantages over classical methods. The NPE posterior shows substantially improved precision \DIFdelbegin \DIFdel{, than a posterior obtained from a comparable ABCanalysis}\DIFdelend \DIFaddbegin \DIFadd{relative to ABC}\DIFaddend , especially for the diffusion parameter $D$, all without the need for manual tolerance tuning. \DIFdelbegin \DIFdel{Furthermore, because }\DIFdelend \DIFaddbegin \DIFadd{Because }\DIFaddend NPE learns directly from the stochastic simulator, it avoids the systematic bias of mean-field approximations and produces a posterior that is not artificially narrow. The resulting credible intervals accurately reflect the natural stochastic variance of the data, a feature often lost when using deterministic surrogate models, cf.\DIFdelbegin \DIFdel{Fig }\DIFdelend \DIFaddbegin \DIFadd{\ Fig~}\DIFaddend \ref{fig:three_figures}. Once the initial network training is complete, posterior samples for any new observation can be obtained almost instantly.
\DIFdelbegin %DIFDELCMD < \begin{figure}[htbp]
%DIFDELCMD < 	\centering
%DIFDELCMD < 	\includegraphics[width=0.8\textwidth]{images/NPE_corner_plot_1d_D}
%DIFDELCMD < 	%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Posterior distribution from NPE using 1D column count data. 
		The figure is arranged as described in Figure \ref{fig:three_figures}. The true parameter values (orange cross) are contained within the 68\% credible interval}}
	%DIFAUXCMD
%DIFDELCMD < \label{fig:npe_1d_posterior}
%DIFDELCMD < \end{figure}
%DIFDELCMD < %%%
\DIFdelend 

\DIFdelbegin \subsubsection{\DIFdel{Predictive Performance}}%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
%DIFDELCMD < \label{sec:posterior_checks}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Beyond parameter estimation, we evaluate NPE's predictive capabilities through posterior predictive sampling. For each posterior sample $\theta^{(j)}$, we generate predicted data $\tilde{x}^{(j)} \sim p(x|\theta^{(j)})$ using the stochastic simulator. The collection of predictions forms empirical prediction intervals that naturally account for both parameter uncertainty and model stochasticity.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{As shown in Figure \ref{fig:npe_1d_predictive}, the resulting NPE-based predictions are well-calibrated}\DIFdelend \DIFaddbegin \DIFadd{Posterior predictive checks confirm that the inferred model reproduces the observed data}\DIFaddend , with 94.8\% of \DIFdelbegin \DIFdel{true }\DIFdelend observations falling within the 95\% prediction intervals. \DIFdelbegin \DIFdel{A key advantage of this simulator-native approach is its physical consistency; }\DIFdelend \DIFaddbegin \DIFadd{Unlike surrogate-based approaches, }\DIFaddend the prediction intervals \DIFdelbegin \DIFdel{respect the non-negative, discrete nature of the count data without requiring an explicit, and potentially erroneous, noise model. Furthermore, the framework naturally captures heteroscedastic uncertainty , with the prediction intervals appropriately widening in regions of high agent density where stochastic variability is greatest}\DIFdelend \DIFaddbegin \DIFadd{are physically consistent---respecting non-negativity and naturally capturing heteroscedastic uncertainty (Appendix~\ref{sec:appendix_ppc})}\DIFaddend .

\begin{figure}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \includegraphics[width=0.8\textwidth]{images/example_predictive_plot_for_paper_1D}
%DIFDELCMD < 	%%%
\DIFdelendFL \DIFaddbeginFL \begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_original_1d}
		\DIFaddendFL \caption{\DIFdelbeginFL \DIFdelFL{Posterior predictive checks from NPE using }\DIFdelendFL 1D column count \DIFdelbeginFL \DIFdelFL{data. The observed data (orange circles) is overlaid on predictions generated by simulating the model with parameters drawn from the inferred }\DIFdelendFL posterior\DIFdelbeginFL \DIFdelFL{distribution. The median (solid red line) and mean (dashed red line) of the predictions are shown, along with the 50\% (dark blue) and 95\% (light blue) prediction intervals. For visualization purposes, a Gaussian kernel has been applied to smooth the predictive distributions. An appropriate proportion of the observed data points lie within the 95\% prediction interval, indicating that the calibrated model is consistent with the data.}\DIFdelendFL }
		\DIFdelbeginFL %DIFDELCMD < \label{fig:npe_1d_predictive}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \label{fig:npe_original_1d}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_original_2d}
		\caption{\DIFaddFL{2D spatial posterior (CNN)}}
		\label{fig:npe_original_2d}
	\end{subfigure}
	\caption{\DIFaddFL{Posterior distributions for the original model with true values $U = 0.3$, $D = 0.175$. (a)~NPE posterior from 1D column count data yields $U = 0.293 \pm 0.008$ and $D = 0.162^{+0.027}_{-0.021}$. (b)~NPE posterior from 2D spatial data using CNN embedding yields $U = 0.294^{+0.011}_{-0.010}$ and $D = 0.156^{+0.026}_{-0.023}$ (medians with 68\% credible intervals). Both approaches recover comparable posteriors, demonstrating that the CNN automatically extracts features from the raw spatial data that are as informative as the curated 1D summary statistics.}}
	\label{fig:npe_original_posteriors}
\DIFaddendFL \end{figure}

\DIFdelbegin \section{\DIFdel{Learning from Raw Spatial Data}}%DIFAUXCMD
\addtocounter{section}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \subsubsection{\DIFadd{NPE with 2D Spatial Data}}\DIFaddend \label{sec:results_2d}

\DIFdelbegin \subsection{\DIFdel{Motivation for Full Spatial Data}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend While column counts provide a natural summary of the spreading process, they necessarily discard information about the vertical distribution of agents. In principle, the spatial correlation structure within the 2D configuration contains additional information about the underlying parameters. For instance, clustering patterns, local density fluctuations, and spatial correlations may help distinguish between different combinations of $P$ and $U$ that produce similar column counts, cf.\DIFaddbegin \DIFadd{\ }\DIFaddend parameter identifiability \footnote{Although parameter identifiability is not a significant concern for the simple model we consider, additional spatial features are of interest for disentangling parameters in more complex models that incorporate processes like cell-cell adhesion or proliferation. We note that for longer times even this simple model becomes non-identifiable in the diffusion coefficient $D$.}. \DIFdelbegin \DIFdel{Traditional inference methods require hand-crafted summary statistics, making it challenging to leverage such high-dimensional spatial information. NPE, however, can learn directly from raw data through automatic feature extraction. While the simple random walk models considered here are likely well-captured by 1D summary statistics, this approach demonstrates the broader potential of NPE for more complex biological systems where spatial structure is critical. }\DIFdelend For example, models incorporating agent-agent interactions, complex boundary conditions, spatially heterogeneous environments, or multi-species dynamics would generate spatial patterns that cannot be adequately summarized by simple column counts.
\DIFdelbegin \DIFdel{In such cases, the ability to learn directly from full spatial data becomes essential for accurate parameter inference. Our framework provides a foundation that can be readily extended to these more sophisticated modelling scenarios without requiring manual specification of appropriate summary statistics.
}\DIFdelend 

\DIFdelbegin \subsection{\DIFdel{Architectural Adaptations for 2D Data}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdel{Processing raw 2D spatial data requires a different architecture than the one used for 1D summary statistics. We treat each lattice configuration from the simulator as a 2D histogram (or count-based image), where each pixel's value represents the integer number of agents $n_{i,j}(t)$ at the corresponding lattice site. To learn informative features directly from this high-dimensional input, we replace the simple feed-forward network with a CNN \citep{lecun1998,krizhevsky2012,goodfellow2016}. The CNN acts as a powerful, learnable feature extractor that is embedded within the NPE pipeline. It uses a series of convolutional and pooling layers to progressively downsample the spatial data into a low-dimensional embedding. This process is designed to capture salient spatial features, such as clustering patterns and local agent densities, while preserving translational invariance. The learned embedding is then passed to the remainder of the network to estimate the posterior distribution. This end-to-end training ensures that the features extracted by the CNN are maximally informative for the specific task of parameter inference. A detailed specification of the CNN architecture, training parameters, and implementation details used in this work can be found in the project GitHub repository.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\subsection{\DIFdel{Parameter Inference from Raw Spatial Data}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Having established a baseline with 1D summary statistics, we now apply our NPE pipeline with the embedded CNN to perform inference directly on the raw 2D spatial data. This approach leverages the full spatial configuration of agents, aiming to extract additional information that may be lost when compressing the data into column counts.
}%DIFDELCMD < 

%DIFDELCMD < %%%
\subsubsection{\DIFdel{Experimental Setup}}
%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
\DIFdelend The experimental setup remains identical to the 1D case, using the same synthetic observation generated with true parameters $P = 0.7$ (\DIFdelbegin \DIFdel{$D=0.7/4$}\DIFdelend \DIFaddbegin \DIFadd{$D = P/4 = 0.175$}\DIFaddend ) and $U = 0.3$. However, instead of using the 201-dimensional vector of column counts as input, the network now receives the full $50 \times 200$ binary image representing the agent locations on the lattice. We use the same broad uniform priors for $P$ and $U$ as \DIFdelbegin \DIFdel{in Section }\DIFdelend \DIFaddbegin \DIFadd{described in Section~}\DIFaddend \ref{sec:NPE}.

\DIFdelbegin \subsubsection{\DIFdel{Parameter Inference Results}}
%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
\DIFdel{Figure \ref{fig:npe_2d_posterior} shows the joint posterior distribution inferred from the 2D spatial data, overlaid with the result from the 1D column count analysis for direct comparison. }\DIFdelend The CNN-based approach \DIFaddbegin \DIFadd{(Figure~\ref{fig:npe_original_2d}) }\DIFaddend recovers a posterior with \DIFdelbegin \DIFdel{means $D = 0.169^{+0.026}_{-0.023}$
 and $U = 0.290^{+0.008}_{-0.09}$}\DIFdelend \DIFaddbegin \DIFadd{medians $D = 0.156^{+0.026}_{-0.023}$
 and $U = 0.294^{+0.011}_{-0.010}$}\DIFaddend . As summarised in Table\DIFaddbegin \DIFadd{~}\DIFaddend \ref{tab:data_comparison}, leveraging the full spatial data yields a posterior with a precision \DIFdelbegin \DIFdel{that is highly }\DIFdelend comparable to that obtained using curated 1D summary statistics, cf.\DIFdelbegin \DIFdel{Section }\DIFdelend \DIFaddbegin \DIFadd{\ Section~}\DIFaddend \ref{sec:1d_results}.

This result demonstrates that the CNN can automatically learn features from the raw data that are as informative as the carefully chosen summary statistics. \DIFdelbegin \DIFdel{This highlights a critical trade-off: the increased computational cost of training the CNN (Table \ref{tab:data_comparison})must be weighed against the major advantage of avoiding manual feature engineering}\DIFdelend \DIFaddbegin \DIFadd{Importantly, the CNN adds negligible overhead to the network training phase compared to the feed-forward embedding used for 1D data (Section~\ref{sec:runtime}), so the primary cost of adopting the 2D pipeline is conceptual rather than computational}\DIFaddend . For more complex models where optimal summary statistics are unknown, this direct-from-pixels approach may be advantageous for accurate inference.

\DIFdelbegin \subsubsection{\DIFdel{Predictive Performance}}
%DIFAUXCMD
\addtocounter{subsubsection}{-1}%DIFAUXCMD
\DIFdel{We assess the predictive performance of the 2D-trained modelusing the same posterior predictive checking procedure as in Section\ref{sec:posterior_checks}.
As shown in Figures \ref{fig:npe_2d_predictive} and \ref{fig:npe_2d_predictive_cnn}, the model generates predictions that are highly consistent with the observed data.
The calibrated model accurately captures the spatial characteristics of the }\DIFdelend \DIFaddbegin \DIFadd{Posterior predictive checks for the 2D model similarly confirm well-calibrated predictions that capture the spatial characteristics of the }\DIFaddend agent-based \DIFdelbegin \DIFdel{simulation, producing prediction intervals that are well-calibrated and physically consistent with the discrete nature of the data}\DIFdelend \DIFaddbegin \DIFadd{simulation (Appendix~\ref{sec:appendix_ppc}).
}




\begin{table}[htbp]
	\centering
	\begin{tabular}{llcc}
		\toprule
		\DIFaddFL{\textbf{Method} }& \DIFaddFL{\textbf{Data Type} }& \DIFaddFL{\textbf{Posterior for $U$} }&  \DIFaddFL{\textbf{Posterior for $D$}  }\\
		\midrule
		\DIFaddFL{True value }& \DIFaddFL{--- }& \DIFaddFL{$0.300$ }& \DIFaddFL{$0.175$ }\\
		\midrule
		\DIFaddFL{ABC }& \DIFaddFL{1D Column counts }& \DIFaddFL{$0.296^{+0.034}_{-0.029}$ }& \DIFaddFL{$0.151^{+0.064}_{-0.061}$ }\\
		\DIFaddFL{Surrogate + MLE/Laplace }& \DIFaddFL{1D Column counts }& \DIFaddFL{$0.314^{+0.004}_{-0.004}$ }& \DIFaddFL{$0.201^{+0.024}_{-0.024}$ }\\
		\DIFaddFL{Surrogate + MCMC }& \DIFaddFL{1D Column counts }& \DIFaddFL{$0.315^{+0.007}_{-0.007}$ }& \DIFaddFL{$0.210^{+0.048}_{-0.047}$ }\\
		\midrule
		\DIFaddFL{NPE }& \DIFaddFL{1D Column counts }& \DIFaddFL{$0.293 \pm 0.008$ }& \DIFaddFL{$0.162^{+0.027}_{-0.021}$}\\
		\DIFaddFL{NPE + CNN }& \DIFaddFL{2D Spatial data }& \DIFaddFL{$0.294^{+0.011}_{-0.010}$ }& \DIFaddFL{$0.156^{+0.026}_{-0.023}$}\\
		\bottomrule
	\end{tabular}
	\caption{\DIFaddFL{Comparison of inference results across methods and data representations. All values show posterior medians with 68\% credible intervals ($x^{+a}_{-b}$); classical method posteriors are extracted from the corner plots in Figure~\ref{fig:three_figures}. The MCMC method additionally infers a noise variance nuisance parameter $\sigma$ (not shown). The surrogate-based methods (Laplace, MCMC) yield biased estimates of $D$, reflecting systematic error from the mean-field approximation.}}
	\label{tab:data_comparison}
\end{table}

\subsubsection{\DIFadd{Summary}}

\DIFadd{Biologically, $U$ represents the initial cell seeding density and $D$ the effective cell motility. For this isotropic model, the comparable precision of 1D and 2D posteriors is expected: column counts capture the symmetric spreading dynamics that encode both parameters. The advantage of spatial analysis emerges for models with directional structure, as we demonstrate next (Section~\ref{sec:model_a}).
}



%DIF > % ---- Model A ----
\subsection{\DIFadd{Model A --- Directional Bias}}\label{sec:model_a}

\DIFadd{Model A extends the isotropic random walk by introducing a directional bias parameter $\rho \in [0,1]$, which controls the probability of stepping in the positive $x$-direction relative to the negative $x$-direction (see Section~\ref{sec:model_description}). This model has three free parameters: the initial occupation probability $U$, the total motility probability $P$, and the bias $\rho$, with true values $U = 0.5$, $P = 0.7$, $\rho = 0.5$}\DIFaddend .
\DIFdelbegin \DIFdel{Importantly, the framework captures the input-dependent (heteroscedastic)uncertainty, something that standard MLE under an i. i. d. \ noise assumption (used in much of the mathematical biology literature) cannot represent }\DIFdelend \DIFaddbegin 

\DIFadd{Using 1D column count data, NPE recovers posteriors $U = 0.498 \pm 0.011$}\DIFaddend , \DIFdelbegin \DIFdel{often leading to miscalibrated, homoscedastic intervals even when the underlying variability is spatially varying.
As in the }\DIFdelend \DIFaddbegin \DIFadd{$P = 0.631 \pm 0.176$, and $\rho = 0.541^{+0.245}_{-0.129}$ (Figure~\ref{fig:model_A_1d}). The initial density $U$ is tightly constrained, while $P$ and $\rho$ exhibit broader posteriors with a strong anti-correlation ($r = -0.962$). This degeneracy is physics-driven: the macroscopic drift velocity $v = P\rho/2$ is well-constrained by the data, but individual values of $P$ and $\rho$ that yield the same drift are interchangeable when viewed through }\DIFaddend 1D \DIFdelbegin \DIFdel{case, the method remains robust when applied to high-dimensional (2D)spatial inputs. }\DIFdelend \DIFaddbegin \DIFadd{column counts alone. That is, the composite quantity $v$ is identifiable, but $P$ and $\rho$ are individually weakly identifiable from 1D data.
}\DIFaddend 

\DIFdelbegin \DIFdel{In the }\DIFdelend \DIFaddbegin \DIFadd{Using full }\DIFaddend 2D \DIFdelbegin \DIFdel{(CNN)results we also note a modest separation between the posterior predictive mean and median, consistent with a mild right-skew in the posterior for $D$.
We attribute the observed skew primarily to structural features of the inference problem—namely a slight $U$}\DIFdelend \DIFaddbegin \DIFadd{spatial data with a CNN embedding network, NPE recovers $U = 0.508 \pm 0.030$, $P = 0.801^{+0.170}_{-0.200}$, and $\rho = 0.416^{+0.147}_{-0.073}$ (Figure~\ref{fig:model_A_2d}). The $P$}\DIFaddend --\DIFaddbegin \DIFadd{$\rho$ anti-correlation persists ($r = -0.924$), despite the CNN having access to the spatial asymmetries that column counts collapse by summing across rows. This confirms that the degeneracy reflects an intrinsic limitation of single-timepoint inference rather than an information loss artefact of the 1D summary. The 2D medians for $P$ and $\rho$ lie in a different region of the degenerate manifold compared to 1D, reflecting the sensitivity of the posterior mode to the data representation when parameters are weakly identifiable. Biologically, $\rho$ quantifies the directional migration preference (e.g.\ chemotactic bias); the strong $P$--$\rho$ coupling means the net directed migration rate is the identifiable and biologically relevant quantity for predicting wound closure direction and speed.
}

\begin{figure*}[htbp]
	\centering
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_A_1d}
		\caption{\DIFaddFL{1D column counts $(U, P, \rho)$}}
		\label{fig:model_A_1d}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_A_2d}
		\caption{\DIFaddFL{2D spatial $(U, P, \rho)$}}
		\label{fig:model_A_2d}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_A_2d_reparam}
		\caption{\DIFaddFL{2D reparameterized $(U, D, v)$}}
		\label{fig:corner_A_reparam}
	\end{subfigure}
	\caption{\DIFaddFL{Posterior distributions for Model~A (directional bias) with true values $U = 0.5$, $P = 0.7$, $\rho = 0.5$. (a,\,b) Both 1D and 2D representations recover $U$ precisely but exhibit a strong $P$--$\rho$ anti-correlation reflecting the drift velocity degeneracy $v = P\rho/2$. (c) Reparameterizing to continuum parameters $(U, D, v)$, where $D = P/4$ and $v = P\rho/2$, transforms the banana-shaped degeneracy into a near-Gaussian posterior, with $v$ constrained to ${\sim}3.4\%$ relative error.}}
	\label{fig:model_A_posteriors}
\end{figure*}

\DIFadd{Since the drift velocity $v = P\rho/2$ is the identifiable quantity, a natural strategy is to reparameterize the NPE to infer continuum parameters $(U, D, v)$ directly, where $D = P/4$ is the diffusivity and $v = P\rho/2$ is the drift velocity. This transformation reuses the same 50}{\DIFadd{,}}\DIFadd{000 training simulations---only the parameter columns are transformed; the observations are unchanged. In the reparameterized space the NPE recovers $U = 0.508 \pm 0.014$, $D = 0.195 \pm 0.021$ (true $0.175$), and $v = 0.169 \pm 0.006$ (true $0.175$), with $v$ constrained to ${\sim}3.4\%$ relative error (Figure~\ref{fig:corner_A_reparam}). The banana-shaped $P$--$\rho$ posterior (Figure~\ref{fig:model_A_2d}) becomes a near-Gaussian }\DIFaddend $D$\DIFdelbegin \DIFdel{trade-off, support constraints ($D\!\ge\!0$ with $D\!=\!P/4$) }\DIFdelend \DIFaddbegin \DIFadd{--$v$ posterior (Figure~\ref{fig:corner_A_reparam}), which normalizing flows can represent faithfully with fewer transforms. This simpler posterior geometry also yields substantially improved SBC diagnostics, as we discuss in Section~\ref{sec:diagnostics}. More broadly, when parameter degeneracies are understood \emph{a~priori}, reparameterizing to physically identifiable quantities offers a simple and effective route to improved flow calibration.
}



%DIF > % ---- Model B ----
\subsection{\DIFadd{Model B --- Proliferation}}\label{sec:model_b}

\DIFadd{Model B incorporates cell proliferation via a Fisher--Kolmogorov mechanism: at each time step, an agent may divide into a daughter cell at an adjacent site with probability $R$, subject to an exclusion constraint that prevents multiple occupancy. This model has three free parameters: the initial occupation probability $U$, the motility probability $P$, and the proliferation rate $R$, with true values $U = 0.5$}\DIFaddend , \DIFaddbegin \DIFadd{$P = 0.7$, and $R = 0.05$.
}

\DIFadd{NPE with 1D column counts recovers $U = 0.353^{+0.148}_{-0.137}$, $P = 0.764^{+0.085}_{-0.081}$, and $R = 0.052 \pm 0.006$ (Figure~\ref{fig:model_B_1d}). While $P$ }\DIFaddend and \DIFdelbegin \DIFdel{the non-linear parameter-to-observable map—together with the fact that the CNN accesses richer spatial cues (e.g., front roughness andlocal clustering)that are averaged out in }\DIFdelend \DIFaddbegin \DIFadd{$R$ are tightly constrained, $U$ exhibits a moderately broad posterior reflecting a partial degeneracy with $R$: higher initial occupancy can mimic the effect of increased proliferation when only a single time point is observed.
}

\DIFadd{With 2D spatial data, NPE recovers $U = 0.345^{+0.290}_{-0.223}$, $P = 0.777^{+0.158}_{-0.129}$, and $R = 0.053^{+0.013}_{-0.009}$ (Figure~\ref{fig:model_B_2d}). The }\DIFaddend 1D \DIFdelbegin \DIFdel{summaries and therefore make such asymmetries more apparent. We cannot entirely rule out minor artefacts arising from the higher-dimensional learning task. }\DIFdelend \DIFaddbegin \DIFadd{posteriors are at least as tight as the 2D posteriors for all three parameters, suggesting that column counts already capture the information relevant to this proliferation model and that the higher-dimensional 2D observation space may slightly impede flow training. Biologically, the ability to jointly infer $U$, $P$, and $R$ from a single experimental snapshot is valuable: in wound healing assays the seeding density may not be precisely controlled, and NPE can recover it alongside the motility and proliferation parameters without additional observations.
}\DIFaddend 

%DIF <  --- Figures and Tables for this section ---
\DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \begin{figure}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \includegraphics[width=0.8\textwidth]{images/NPE_corner_plot_2d_D_v2}
%DIFDELCMD < 	\caption{%%%
\DIFdelFL{As Figure \ref{fig:npe_1d_posterior}}\DIFdelendFL \DIFaddbeginFL \begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_B_1d}
		\caption{\DIFaddFL{1D column count posterior}}
		\label{fig:model_B_1d}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_B_2d}
		\caption{\DIFaddFL{2D spatial posterior (CNN)}}
		\label{fig:model_B_2d}
	\end{subfigure}
	\caption{\DIFaddFL{Posterior distributions for Model B (proliferation) with true values $U = 0.5$, $P = 0.7$, $R = 0.05$. Both 1D and 2D representations recover $P$ and $R$ precisely, while $U$ exhibits a partial degeneracy with $R$ reflecting the interchangeability of initial density and proliferation at a single time point.}}
	\label{fig:model_B_posteriors}
\end{figure}



%DIF > % ---- Model C ----
\subsection{\DIFadd{Model C --- Combined Bias and Proliferation}}\label{sec:model_c}

\DIFadd{Model C combines directional bias and proliferation, representing the most biologically realistic scenario in our suite. Unlike Models~A and~B, which each introduce one additional mechanism, Model~C incorporates both bias and proliferation simultaneously, yielding a four-parameter inference problem: the initial occupation probability $U$, the motility probability $P$, the directional bias $\rho$, and the proliferation rate $R$, with true values $U = 0.5$}\DIFaddend , \DIFdelbegin \DIFdel{with posteriors generated from the NPE with CNN embedding networks}\DIFdelend \DIFaddbegin \DIFadd{$P = 0.7$}\DIFaddend , \DIFdelbegin \DIFdel{using full }\DIFdelend \DIFaddbegin \DIFadd{$\rho = 0.5$, $R = 0.05$. Although a mean-field PDE can in principle be written for this combination of mechanisms, the interplay of bias, proliferation, and exclusion effects means that the surrogate approximation is no longer well validated, and the additional requirement of specifying an appropriate noise model further compounds the modelling uncertainty. This model therefore represents the strongest case for simulation-based inference, which bypasses surrogate approximations entirely.
}

\DIFadd{NPE with 1D column counts recovers $U = 0.496^{+0.116}_{-0.113}$, $P = 0.671 \pm 0.073$, $\rho = 0.485^{+0.057}_{-0.052}$, and $R = 0.052 \pm 0.003$ (Figure~\ref{fig:model_C_1d}). Despite the four-parameter space, $R$ is recovered with ${\sim}6\%$ relative uncertainty, and the $P$--$\rho$ anti-correlation mirrors the degeneracy observed in Model~A.
}

\DIFadd{With }\DIFaddend 2D \DIFdelbegin \DIFdel{spatial data.}%DIFDELCMD < \MBLOCKRIGHTBRACE
%DIFDELCMD < 	\label{fig:npe_2d_posterior}
%DIFDELCMD < \end{figure}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \DIFadd{spatial data, NPE recovers $U = 0.354^{+0.125}_{-0.119}$, $P = 0.743^{+0.066}_{-0.059}$, $\rho = 0.390^{+0.039}_{-0.037}$, and $R = 0.055^{+0.006}_{-0.004}$ (Figure~\ref{fig:model_C_2d}). The 2D posteriors are tighter for $P$ and $\rho$, suggesting that spatial information provides additional constraining power for these parameters, while $U$ and $R$ are comparably constrained by column counts alone. Biologically, these four parameters represent the key processes governing wound healing dynamics: initial cell density, motility, directional migration, and cell division, all inferred simultaneously from a single experimental snapshot.
}\DIFaddend 

\begin{figure}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \includegraphics[width=0.8\textwidth]{images/example_predictive_plot_for_paper_2D}
%DIFDELCMD < 	%%%
\DIFdelendFL \DIFaddbeginFL \begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_C_1d}
		\caption{\DIFaddFL{1D column count posterior}}
		\label{fig:model_C_1d}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/corner_C_2d}
		\caption{\DIFaddFL{2D spatial posterior (CNN)}}
		\label{fig:model_C_2d}
	\end{subfigure}
	\caption{\DIFaddFL{Posterior distributions for Model~C (bias + proliferation) with true values $U = 0.5$, $P = 0.7$, $\rho = 0.5$, $R = 0.05$. The $P$--$\rho$ anti-correlation from Model~A persists, and $R$ remains tightly constrained despite the expanded parameter space. The 2D representation yields tighter posteriors for $P$ and $\rho$.}}
	\label{fig:model_C_posteriors}
\end{figure}



%DIF > % ---- SBI Diagnostics ----
\subsection{\DIFadd{SBI Diagnostics}}\label{sec:diagnostics}

\DIFadd{Following the diagnostic protocol recommended by \cite{Cranmer2020FrontierSBI} and \cite{2025arXiv250812939D}, we validate all trained posteriors using three complementary diagnostics: the Kolmogorov--Smirnov (KS) test for marginal calibration, the classifier two-sample test (C2ST) for posterior quality, and Tests of Accuracy with Random Points (TARP) for coverage calibration.
}

\DIFadd{Table~\ref{tab:diagnostics} summarises the diagnostic results for both 1D and 2D posteriors across all models. The C2ST scores are consistently close to $0.5$ across all models, parameters, and data representations, indicating that the learned posteriors are nearly indistinguishable from the true posteriors and that the density estimators have been well-trained. The TARP area-under-the-curve (ATC) metric passes for the Original model and Model~A in both 1D and 2D (ATC values near zero), while Models~B and~C show larger deviations in the 1D setting (ATC $= -0.44$ and $-0.54$, respectively), indicating moderate miscalibration for the proliferation models.
}

\DIFadd{For the 1D posteriors, the KS test results are mixed across models. In the Original model, $U$ passes comfortably ($p = 0.96$) while $P$ fails ($p = 0.0001$). In Model~A, $U$ fails ($p = 0.0005$), $P$ passes marginally ($p = 0.07$), and $\rho$ passes ($p = 0.15$). Model~B shows KS failures for all three parameters ($U$, $P$, and $R$), and Model~C fails for $U$, $P$, and $R$ while $\rho$ passes ($p = 0.36$). The KS failures are concentrated in the proliferation models (B and C), where the SBC rank ECDFs (Figure~\ref{fig:sbc_plots}c--d) show systematic deviations characteristic of mild overconfidence: the learned posteriors are precise but slightly too narrow, so that the true value falls in the tails more often than expected. This likely reflects the nonlinear and relatively flat mapping from parameters to observable column counts in the proliferation models: modest changes in $R$ or $U$ produce only subtle differences in the data, making it difficult for the normalizing flow to accurately learn the posterior width.
}

\DIFadd{The 2D posteriors exhibit broadly similar calibration patterns (dashed lines in Figure~\ref{fig:sbc_plots}), with C2ST scores remaining near 0.5 throughout. The KS test is somewhat more sensitive in the 2D setting: the Original model shows failures for both $U$ and $P$; Model~A fails for all three parameters ($U$, $P$, and $\rho$); Model~B shows a marginal failure for $R$; and Model~C fails for $U$ and $\rho$ while $P$ and $R$ pass. These failures likely reflect the challenge of learning accurate posterior widths in the higher-dimensional observation space. For Model~A, where $P$ and $\rho$ are strongly degenerate, the KS failures may additionally reflect the CNN concentrating posterior mass on a different region of the degenerate manifold than the 1D embedding, as evidenced by the shifted marginal medians (Section~\ref{sec:model_a}).
}

\DIFadd{This tendency to underestimate posterior variance is a recognised challenge across simulation-based inference methods more broadly \citep{hermans2022crisis}, though amortized approaches such as NPE tend to be more conservative and more simulation-efficient than their non-amortized counterparts. The KS test is highly sensitive to subtle deviations in the tails of the rank distribution, which may not affect practical inference quality; by contrast, the C2ST diagnostic passes consistently across all models and parameters, providing stronger evidence that the learned posteriors are suitable for practical use. Increasing the training budget or employing sequential refinement strategies could improve KS calibration further, but the miscalibration observed here is mild, with curves remaining close to the DKW confidence bands.
}

\begin{table}[htbp]
	\centering
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{@{}llcccccc@{}}
		\toprule
		& & \multicolumn{3}{c}{\textbf{1D NPE}} & \multicolumn{3}{c}{\textbf{2D NPE (CNN)}} \\
		\cmidrule\DIFaddFL{(lr)}{\DIFaddFL{3-5}} \cmidrule\DIFaddFL{(lr)}{\DIFaddFL{6-8}}
		\DIFaddFL{\textbf{Model} }& \DIFaddFL{\textbf{Param.} }& \DIFaddFL{\textbf{KS $p$} }& \DIFaddFL{\textbf{C2ST} }& \DIFaddFL{\textbf{ATC} }& \DIFaddFL{\textbf{KS $p$} }& \DIFaddFL{\textbf{C2ST} }& \DIFaddFL{\textbf{ATC} }\\
		\midrule
		\DIFaddFL{Original }& \DIFaddFL{$U$ }& \DIFaddFL{0.96 }& \DIFaddFL{0.59 }& \multirow{2}{*}{0.05} & \DIFaddFL{0.0001}\rlap{$^*$} & \DIFaddFL{0.58 }& \multirow{2}{*}{$-0.41$} \\
		         & \DIFaddFL{$P$ }& \DIFaddFL{0.0001}\rlap{$^*$} & \DIFaddFL{0.58 }& & \DIFaddFL{0.02}\rlap{$^*$} & \DIFaddFL{0.59 }& \\
		\midrule
		\DIFaddFL{A        }& \DIFaddFL{$U$    }& \DIFaddFL{0.0005}\rlap{$^*$} & \DIFaddFL{0.57 }& \multirow{3}{*}{$-0.05$} & \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.64 }& \multirow{3}{*}{0.08} \\
		         & \DIFaddFL{$P$    }& \DIFaddFL{0.07   }& \DIFaddFL{0.56 }& & \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.56 }& \\
		         & \DIFaddFL{$\rho$ }& \DIFaddFL{0.15   }& \DIFaddFL{0.57 }& & \DIFaddFL{0.02}\rlap{$^*$} & \DIFaddFL{0.58 }& \\
		\midrule
		\DIFaddFL{B        }& \DIFaddFL{$U$ }& \DIFaddFL{0.009}\rlap{$^*$} & \DIFaddFL{0.58 }& \multirow{3}{*}{$-0.44$} & \DIFaddFL{0.11 }& \DIFaddFL{0.57 }& \multirow{3}{*}{$-0.36$} \\
		         & \DIFaddFL{$P$ }& \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.59 }& & \DIFaddFL{0.41 }& \DIFaddFL{0.57 }& \\
		         & \DIFaddFL{$R$ }& \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.57 }& & \DIFaddFL{0.009}\rlap{$^*$} & \DIFaddFL{0.59 }& \\
		\midrule
		\DIFaddFL{C        }& \DIFaddFL{$U$    }& \DIFaddFL{0.03}\rlap{$^*$} & \DIFaddFL{0.56 }& \multirow{4}{*}{$-0.54$} & \DIFaddFL{0.004}\rlap{$^*$} & \DIFaddFL{0.58 }& \multirow{4}{*}{$-0.12$} \\
		         & \DIFaddFL{$P$    }& \DIFaddFL{0.02}\rlap{$^*$} & \DIFaddFL{0.56 }& & \DIFaddFL{0.07 }& \DIFaddFL{0.58 }& \\
		         & \DIFaddFL{$\rho$ }& \DIFaddFL{0.36 }& \DIFaddFL{0.59 }& & \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.58 }& \\
		         & \DIFaddFL{$R$    }& \DIFaddFL{0.0000}\rlap{$^*$} & \DIFaddFL{0.56 }& & \DIFaddFL{0.22 }& \DIFaddFL{0.58 }& \\
		\bottomrule
	\end{tabular}
	\DIFaddendFL \caption{\DIFdelbeginFL \DIFdelFL{Posterior predictive check }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Diagnostic summary }\DIFaddendFL for \DIFdelbeginFL \DIFdelFL{the }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{1D and }\DIFaddendFL 2D \DIFdelbeginFL \DIFdelFL{spatial data model}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{NPE posteriors across all models}\DIFaddendFL . \DIFdelbeginFL \DIFdelFL{The format is identical to Figure \ref{fig:npe_1d_predictive}. The predictions are well-calibrated and consistent }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{KS $p$-values marked }\DIFaddendFL with \DIFdelbeginFL \DIFdelFL{the observed data}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$^*$ indicate failure ($p < 0.05$)}\DIFaddendFL . \DIFaddbeginFL \DIFaddFL{C2ST scores near 0.5 indicate well-learned posteriors. TARP ATC values near zero indicate good calibration.}\DIFaddendFL }
	\DIFdelbeginFL %DIFDELCMD < \label{fig:npe_2d_predictive}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \label{tab:diagnostics}
\end{table}

\begin{figure}[htbp]
	\centering
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sbc_original}
		\caption{\DIFaddFL{Original model}}
		\label{fig:sbc_original}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sbc_model_A}
		\caption{\DIFaddFL{Model A (directional bias)}}
		\label{fig:sbc_model_A}
	\end{subfigure}

	\vspace{0.5cm}

	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sbc_model_B}
		\caption{\DIFaddFL{Model B (proliferation)}}
		\label{fig:sbc_model_B}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sbc_model_C}
		\caption{\DIFaddFL{Model C (bias + proliferation)}}
		\label{fig:sbc_model_C}
	\end{subfigure}
	\caption{\DIFaddFL{Simulation-based calibration (SBC) rank ECDF plots for all four models. Solid lines show 1D NPE posteriors; dashed lines show 2D NPE (CNN) posteriors. A uniform rank distribution (diagonal within the shaded 99\% DKW confidence bands) indicates well-calibrated posteriors; an ECDF bowing below the diagonal indicates mild overconfidence. (a)~Original model: $U$ is well-calibrated in 1D while $P$ deviates; 2D posteriors show deviations for both parameters. (b)~Model A: $U$ fails in both representations; $P$ and $\rho$ pass in 1D, while all three parameters fail in 2D. (c)~Model B: all three parameters ($U$, $P$, $R$) fail the KS test in 1D; $R$ shows marginal deviation in 2D. (d)~Model C: the four-parameter space shows KS failures for $U$, $P$, and $R$ in 1D (with $\rho$ passing), and for $U$ and $\rho$ in 2D.}}
	\label{fig:sbc_plots}
\DIFaddendFL \end{figure}

\DIFaddbegin \DIFadd{When parameter degeneracies are understood \emph{a~priori}, reparameterizing to identifiable quantities can substantially improve calibration. For Model~A, reparameterizing from lattice parameters $(U, P, \rho)$ to continuum parameters $(U, D, v)$ (Section~\ref{sec:model_a}) yields markedly better diagnostics (Figure~\ref{fig:sbc_reparam}): $D$ now passes the KS test ($p = 0.60$, versus $p < 0.001$ for $P$ in the baseline), the $U$ C2ST score improves from $0.64$ to $0.60$, the TARP ATC halves from $0.084$ to $0.035$, and all six C2ST tests pass (versus five of six in the baseline). The improvement arises because the reparameterization transforms the curved $P$--$\rho$ banana into a near-Gaussian $D$--$v$ posterior that the normalizing flow represents more faithfully.
}

\DIFaddend \begin{figure}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \includegraphics[width=0.8\textwidth]{images/example_predictive_plot_for_paper_2D_CNN}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \includegraphics[width=\columnwidth]{images/sbc_model_A_reparam_comparison}
	\DIFaddendFL \caption{\DIFdelbeginFL \DIFdelFL{Posterior predictive check }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{SBC rank ECDFs }\DIFaddendFL for \DIFdelbeginFL \DIFdelFL{the }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Model~A (}\DIFaddendFL 2D\DIFdelbeginFL \DIFdelFL{spatial data model, showing the full observed data points }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{): baseline parameterization $(U, P, \rho)$ }\DIFaddendFL (\DIFdelbeginFL \DIFdelFL{orange}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{left}\DIFaddendFL ) \DIFdelbeginFL \DIFdelFL{overlaid on the predicted occupancy probability from the NPE model }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{versus reparameterized $(U, D, v)$ }\DIFaddendFL (\DIFdelbeginFL \DIFdelFL{blue heat-map}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{right}\DIFaddendFL ). The \DIFdelbeginFL \DIFdelFL{grey contours delineate iso-probability curves at probability levels of 0.05, 0.10, and 0.20 }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{reparameterization substantially improves calibration: $D$ passes the KS test }\DIFaddendFL (\DIFdelbeginFL \DIFdelFL{dotted}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{versus a failure for $P$)}\DIFaddendFL , \DIFdelbeginFL \DIFdelFL{dashed}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{the TARP ATC halves}\DIFaddendFL , \DIFdelbeginFL \DIFdelFL{solid, respectively)}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{and all C2ST tests pass}\DIFaddendFL . \DIFaddbeginFL \DIFaddFL{Shaded bands show the 95\% DKW confidence envelope.}\DIFaddendFL }
	\DIFdelbeginFL %DIFDELCMD < \label{fig:npe_2d_predictive_cnn}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \label{fig:sbc_reparam}
\DIFaddendFL \end{figure}

%DIF > % ---- Comprehensive Results Table ----
\DIFaddbegin 

\DIFadd{Table~\ref{tab:all_results} provides a comprehensive summary of NPE inference results across all four models and both data representations.
}

\DIFaddend \begin{table}[htbp]
	\centering
	\DIFdelbeginFL %DIFDELCMD < \begin{tabular}{lcc}
%DIFDELCMD < 		%%%
\DIFdelendFL \DIFaddbeginFL \renewcommand{\arraystretch}{1.2}
	\begin{tabular}{@{}llccc@{}}
		\DIFaddendFL \toprule
		\textbf{\DIFdelbeginFL \DIFdelFL{Data Type}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Model}\DIFaddendFL } & \textbf{\DIFdelbeginFL \DIFdelFL{Posterior for $U$}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Parameter}\DIFaddendFL } & \textbf{\DIFdelbeginFL \DIFdelFL{Posterior for $D$}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{True Value}\DIFaddendFL } \DIFaddbeginFL & \DIFaddFL{\textbf{1D NPE} }& \DIFaddFL{\textbf{2D NPE} }\DIFaddendFL \\
		\midrule
		\DIFdelbeginFL \DIFdelFL{1D Column counts  }\DIFdelendFL \DIFaddbeginFL \multirow{2}{*}{Original} \DIFaddendFL & \DIFdelbeginFL \DIFdelFL{$0.298^{+0.009}_{-0.008}$ }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{$U$ }\DIFaddendFL & \DIFdelbeginFL \DIFdelFL{$0.164^{+0.024}_{-0.024}$}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{0.300 }& \DIFaddFL{$0.293 \pm 0.008$ }& \DIFaddFL{$0.294^{+0.011}_{-0.010}$ }\DIFaddendFL \\
		                          \DIFaddbeginFL & \DIFaddFL{$D$ }& \DIFaddFL{0.175 }& \DIFaddFL{$0.162^{+0.027}_{-0.021}$ }& \DIFaddFL{$0.156^{+0.026}_{-0.023}$ }\\
		\midrule
		\multirow{3}{*}{A (Bias)} & \DIFaddFL{$U$    }& \DIFaddFL{0.500 }& \DIFaddFL{$0.498 \pm 0.011$ }& \DIFaddFL{$0.508 \pm 0.030$ }\\
		                          & \DIFaddFL{$P$    }& \DIFaddFL{0.700 }& \DIFaddFL{$0.631 \pm 0.176$ }& \DIFaddFL{$0.801^{+0.170}_{-0.200}$ }\\
		                          & \DIFaddFL{$\rho$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.541^{+0.245}_{-0.129}$ }& \DIFaddFL{$0.416^{+0.147}_{-0.073}$ }\\
		\midrule
		\multirow{3}{*}{B (Proliferation)} & \DIFaddFL{$U$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.353^{+0.148}_{-0.137}$ }& \DIFaddFL{$0.345^{+0.290}_{-0.223}$ }\\
		                                   & \DIFaddFL{$P$ }& \DIFaddFL{0.700 }& \DIFaddFL{$0.764^{+0.085}_{-0.081}$ }& \DIFaddFL{$0.777^{+0.158}_{-0.129}$ }\\
		                                   & \DIFaddFL{$R$ }& \DIFaddFL{0.050 }& \DIFaddFL{$0.052 \pm 0.006$ }& \DIFaddFL{$0.053^{+0.013}_{-0.009}$ }\\
		\midrule
		\multirow{4}{*}{C (Bias + Prolif.)} & \DIFaddFL{$U$    }& \DIFaddFL{0.500 }& \DIFaddFL{$0.496^{+0.116}_{-0.113}$ }& \DIFaddFL{$0.354^{+0.125}_{-0.119}$ }\\
		                                    & \DIFaddFL{$P$    }& \DIFaddFL{0.700 }& \DIFaddFL{$0.671 \pm 0.073$ }& \DIFaddFL{$0.743^{+0.066}_{-0.059}$ }\\
		                                    & \DIFaddFL{$\rho$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.485^{+0.057}_{-0.052}$ }& \DIFaddFL{$0.390^{+0.039}_{-0.037}$ }\\
		                                    & \DIFaddFL{$R$    }& \DIFaddFL{0.050 }& \DIFaddFL{$0.052 \pm 0.003$ }& \DIFaddFL{$0.055^{+0.006}_{-0.004}$ }\\
		\bottomrule
	\end{tabular}
	\caption{\DIFaddFL{Comprehensive NPE inference results across all models. Values show posterior medians with 68\% credible intervals. For the original model, $D = P/4$ is reported for comparison with classical methods. All models use broad uniform priors on the unit interval for probability parameters and $R \sim \mathcal{U}(0.001, 0.2)$ for the proliferation rate.}}
	\label{tab:all_results}
\end{table}


\subsection{\DIFadd{Computational Performance}}\label{sec:runtime}

\DIFadd{To provide a practical comparison of computational costs, Table~\ref{tab:runtimes} separates the NPE cost into simulation generation and neural network training. Simulation generation is the dominant expense, accounting for approximately 74\% of the total time for the original model and over 96\% for the exclusion models~A--C. This dominance arises because each training simulation involves running the full stochastic agent-based model: the original model generates $\sim$20 simulations per second, while the exclusion models produce only $\sim$2 per second due to the additional interaction logic. In contrast, neural network training is fast ($\sim$2--3~minutes on an A100 GPU) and largely independent of model complexity, since it operates on the already-generated data tensors. The choice of 1D column-count versus }\DIFaddend 2D \DIFdelbegin \DIFdel{Spatial data }\DIFdelend \DIFaddbegin \DIFadd{spatial-grid output has negligible impact on simulation time, as the underlying stochastic simulation is identical; only the output format differs. The CNN-based 2D pipeline also adds negligible overhead to the network training phase: for a fixed training set size, CNN and feed-forward network training times are comparable on GPU hardware. These figures are illustrative of a typical workflow and are not the result of systematic hyper-parameter optimisation. The simulation budget (here 50}{\DIFadd{,}}\DIFadd{000 samples) scales the generation cost proportionally, with an uninvestigated trade-off in posterior accuracy. Similarly, ABC runtime depends on the number of simulations generated (here, $10^5$). Other hyper-parameters (e.g.\ number of network epochs, batch size, learning rate, width/depth of the embedding network) also have a direct influence on both runtime and accuracy, but were fixed to reasonable defaults rather than tuned for performance.
}

\begin{table}[htbp]
	\centering
	\renewcommand{\arraystretch}{1.2} %DIF >  for better row spacing
	\begin{tabular}{@{}llccc@{}}
		\toprule
		\DIFaddFL{\textbf{Method} }\DIFaddendFL & \DIFdelbeginFL \DIFdelFL{$0.290^{+0.008}_{-0.009}$ }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{\textbf{Model} }\DIFaddendFL & \DIFdelbeginFL \DIFdelFL{$0.169^{+0.026}_{-0.023}$}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{\textbf{Sim.\ Generation (s)} }& \DIFaddFL{\textbf{NN Training (s)} }& \DIFaddFL{\textbf{Inference (s)} }\DIFaddendFL \\
		\DIFaddbeginFL \midrule
		\DIFaddFL{ABC }& \DIFaddFL{Stochastic Simulator }& \DIFaddFL{-- }& \DIFaddFL{-- }& \DIFaddFL{$\sim$2000 }\\
		\DIFaddFL{ABC }& \DIFaddFL{Surrogate Model }& \DIFaddFL{-- }& \DIFaddFL{-- }& \DIFaddFL{$\sim$2 }\\
		\DIFaddFL{MCMC }& \DIFaddFL{Surrogate Model }& \DIFaddFL{-- }& \DIFaddFL{-- }& \DIFaddFL{$\sim$1 }\\
		\midrule
		\DIFaddFL{NPE }& \DIFaddFL{Original }& \DIFaddFL{$\sim$515 }& \DIFaddFL{$\sim$175 }& \DIFaddFL{$<$1 }\\
		\DIFaddFL{NPE }& \DIFaddFL{Model~A }& \DIFaddFL{$\sim$4325 }& \DIFaddFL{$\sim$135 }& \DIFaddFL{$<$1 }\\
		\DIFaddFL{NPE }& \DIFaddFL{Model~B }& \DIFaddFL{$\sim$4925 }& \DIFaddFL{$\sim$125 }& \DIFaddFL{$<$1 }\\
		\DIFaddFL{NPE }& \DIFaddFL{Model~C }& \DIFaddFL{$\sim$4855 }& \DIFaddFL{$\sim$170 }& \DIFaddFL{$<$1 }\\
		\DIFaddendFL \bottomrule
	\end{tabular}
		\caption{\DIFdelbeginFL \DIFdelFL{Comparison of NPE performance using different data representations}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Computational runtimes for various inference methods}\DIFaddendFL . \DIFdelbeginFL \DIFdelFL{Values show posterior medians with 68\% credible intervals }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{NPE costs are separated into simulation generation }\DIFaddendFL (\DIFdelbeginFL \DIFdelFL{$x^{+a}_{-b}$}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{50}{\DIFaddFL{,}}\DIFaddFL{000 training simulations on 16~CPU workers}\DIFaddendFL ) \DIFaddbeginFL \DIFaddFL{and neural network training (on an A100 GPU)}\DIFaddendFL . \DIFdelbeginFL \DIFdelFL{Inference using the }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{Simulation time is independent of output format (1D column counts vs.\ }\DIFaddendFL 2D spatial \DIFdelbeginFL \DIFdelFL{data yields comparable posterior precision to }\DIFdelendFL \DIFaddbeginFL \DIFaddFL{grids), as }\DIFaddendFL the \DIFdelbeginFL \DIFdelFL{1D summary statistics}\DIFdelendFL \DIFaddbeginFL \DIFaddFL{underlying stochastic simulation is identical}\DIFaddendFL . \DIFaddbeginFL \DIFaddFL{Simulation generation dominates the cost, accounting for $\sim$74\% of the total for the original model and over 96\% for the exclusion models~A--C. Once trained, NPE inference for 5}{\DIFaddFL{,}}\DIFaddFL{000 posterior samples takes under one second.}\DIFaddendFL }	\DIFdelbeginFL %DIFDELCMD < \label{tab:data_comparison}
%DIFDELCMD < %%%
\DIFdelendFL \DIFaddbeginFL \label{tab:runtimes}
\DIFaddendFL \end{table}

\DIFaddbegin \DIFadd{Two distinct computational strategies emerge from this comparison. Surrogate-based approaches are extremely fast because they entirely bypass repeated stochastic simulations, relying instead on precomputed or deterministic approximations of the model. In contrast, the NPE framework demands a substantial one-time computational investment, dominated by simulation generation rather than network training, but then performs posterior inference for new observations almost instantaneously. This separation is practically important: the simulation bottleneck is inherent to the stochastic model and can be mitigated through parallelism or more efficient simulators, whereas the network training component is already fast and scales well on modern GPU hardware. The amortized structure makes NPE particularly attractive for scenarios requiring repeated or real-time inference across many datasets.
}


%DIF > % ============================================================
%DIF > %  4. DISCUSSION
%DIF > % ============================================================
\DIFaddend \section{Discussion}\label{sec:discussion}

\subsection{Advantages and Limitations of Neural Posterior Estimation}

NPE offers a robust and flexible framework for parameter inference, directly addressing several challenges that have limited the practical application of stochastic agent-based models. \DIFdelbegin \DIFdel{NPE eliminates the need for surrogate models by }\DIFdelend \DIFaddbegin \DIFadd{By }\DIFaddend learning directly from the full stochastic simulator\DIFaddbegin \DIFadd{, it eliminates the need for surrogate models}\DIFaddend . This direct approach circumvents the systematic bias introduced by mean-field approximations and removes the challenging requirement of specifying explicit noise models that connect deterministic surrogates to stochastic observations. \DIFdelbegin \DIFdel{The method's ability to process }\DIFdelend \DIFaddbegin \DIFadd{Normalizing flows can also capture complex, multimodal posterior geometries that arise naturally from model non-identifiabilities without requiring restrictive parametric assumptions about posterior shape. Processing }\DIFaddend raw high-dimensional data through automatic feature extraction \DIFdelbegin \DIFdel{represents another advantage, avoiding }\DIFdelend \DIFaddbegin \DIFadd{avoids }\DIFaddend the information loss inherent in manually crafted summary statistics while enabling the analysis of complex spatial patterns that would be difficult to characterize a priori. \DIFdelbegin \DIFdel{The amortized nature of NPE provides substantial computational advantages once training is complete. After }\DIFdelend \DIFaddbegin \DIFadd{NPE is also amortized: after }\DIFaddend the initial investment in generating training data and optimizing the neural network, posterior inference for new observations requires \DIFdelbegin \DIFdel{only milliseconds }\DIFdelend \DIFaddbegin \DIFadd{under a second }\DIFaddend of network evaluation rather than \DIFdelbegin \DIFdel{hours (or more) of }\DIFdelend \DIFaddbegin \DIFadd{the minutes to hours required by }\DIFaddend simulation-based methods like ABC.
\DIFdelbegin \DIFdel{This efficiency enables real-time analysis and interactive exploration of the parameter space that would be prohibitive with classical approaches. Furthermore, normalizing flows can capture complex, multimodal posterior geometries that arise naturally from model non-identifiabilities without requiring restrictive parametric assumptions about posterior shape.
}\DIFdelend 

However, NPE also introduces new challenges and limitations that practitioners must carefully consider. The method's appetite for training data can be substantial, particularly for high-dimensional parameter spaces where the curse of dimensionality becomes prohibitive. Problems with more than \DIFdelbegin \DIFdel{15-20 }\DIFdelend \DIFaddbegin \DIFadd{15--20 }\DIFaddend parameters may require simulation budgets that exceed practical computational limits. \DIFdelbegin \DIFdel{Additionally, }\DIFdelend NPE performance can \DIFaddbegin \DIFadd{also }\DIFaddend degrade significantly when test observations fall outside the training distribution, making careful prior specification and robust training data generation critical for reliable inference.

The neural network architecture introduces another layer of complexity, as design choices regarding depth, width, and flow type can dramatically impact performance. Unlike classical methods with well-established theoretical foundations, NPE provides limited formal guarantees about convergence or approximation quality. This black-box nature can make diagnosis of poor performance challenging and requires practitioners to develop intuition about network behaviour and training dynamics.

\DIFdelbegin \subsection{\DIFdel{Spatial information}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdel{Our results highlight a critical trade-off between methodological complexity and computational cost. For }\DIFdelend \DIFaddbegin \DIFadd{Our model hierarchy illustrates a spectrum from convenience to necessity for simulation-based inference. For the original isotropic model, classical surrogates exist and NPE is primarily a computational convenience---it replaces per-observation ABC costs with a one-time training investment. For Model~A (directional bias) and Model~B (proliferation), surrogates become increasingly approximate, and NPE offers both computational and accuracy advantages by avoiding mean-field approximations. For Model~C (combined bias and proliferation), the interplay of multiple mechanisms renders mean-field surrogates difficult to validate, and NPE provides a principled route to full posterior inference by bypassing the surrogate entirely. This progression underscores NPE's role as a general-purpose inference engine that scales naturally with model complexity, performing well across all four models regardless of whether a reliable surrogate happens to exist.
}

\DIFadd{NPE is one of several neural approaches to simulation-based inference \citep{Cranmer2020FrontierSBI,2025arXiv250812939D}. Neural Ratio Estimation (NRE) learns the likelihood-to-evidence ratio and requires MCMC sampling to obtain posterior samples \citep{hermans2020likelihood}, while Neural Likelihood Estimation (NLE) learns a surrogate likelihood function that must likewise be combined with MCMC \citep{papamakarios2019sequential}. We chose NPE because it directly produces the posterior density without any additional sampling step, yielding a self-contained inference procedure that requires no MCMC post-processing. Benchmark studies in low-to-moderate-dimensional problems show that NPE performs competitively with NRE and NLE in terms of posterior accuracy \citep{lueckmann2021benchmarking}. As a practical bonus, }\DIFaddend the \DIFdelbegin \DIFdel{simple random walk model, well-chosen summary statistics provide comparable precision to the full spatial analysis at a fraction of }\DIFdelend \DIFaddbegin \DIFadd{learned posterior is amortized: once trained, each new observation yields posterior samples via a single forward pass, which is advantageous when analysing multiple experimental replicates. We note that NRE or NLE may be preferable for problems where }\DIFaddend the \DIFdelbegin \DIFdel{cost, making them a highly effective and efficient choice. However, this should not obscure the broader potential of spatial inference approaches; the cost-benefit calculation shifts decisively toward spatial approaches for more complex biological systems where crucial information is encoded in patterns that resist simple summarization}\DIFdelend \DIFaddbegin \DIFadd{likelihood-to-evidence ratio is simpler to learn than the full posterior}\DIFaddend , or where \DIFdelbegin \DIFdel{informative statistics are not known a priori}\DIFdelend \DIFaddbegin \DIFadd{sequential refinement around a single observation is prioritised over amortization across many observations}\DIFaddend .

\DIFdelbegin \DIFdel{In many realistic scenarios, crucial mechanistic information is encoded in intricate spatial patterns that are lost in simple one-dimensional projections. For example, models incorporating }\DIFdelend \DIFaddbegin \subsection{\DIFadd{Role of Spatial Information}}
\DIFadd{Our results reveal a nuanced picture of when spatial information improves inference. For the original isotropic model, 1D column counts and 2D spatial data yield comparable posterior precision (Table~\ref{tab:all_results}), confirming that well-chosen summary statistics are effective when the model's symmetry aligns with the summary statistic. This framing clarifies the role of each data representation: 1D column counts are hand-crafted summary statistics that exploit known model symmetries, while the 2D CNN performs automated feature extraction directly from raw spatial data. For isotropic models, the CNN learns features functionally equivalent to column counts; the two approaches converge because no additional spatial information exists to exploit.
}

\DIFadd{The value of spatial inference is demonstrated concretely by the extended models. Model A (directional bias) creates anisotropic spreading patterns where agents preferentially migrate in one direction, producing spatial asymmetries that are collapsed when column counts sum across rows. The 2D CNN has access to these directional features, and while the $P$--$\rho$ degeneracy persists in both representations (Section~\ref{sec:model_a}), the spatial approach provides a representation that naturally accommodates anisotropy without requiring the analyst to design bias-aware summary statistics. When comparing matched training budgets (50}{\DIFadd{,}}\DIFadd{000 simulations for both 1D and 2D), the benefits of spatial data are parameter-dependent. In Model~A, the 2D posterior for $\rho$ is approximately 1.7 times tighter than 1D ($\sim$0.59$\times$ the CI width), reflecting the spatial asymmetries created by directional bias, while $P$ is comparably constrained by both representations. In Model~B, the 1D posteriors are at least as tight as the 2D posteriors for all three parameters, indicating that column counts already capture the information relevant to this proliferation model. In Model~C, the picture is mixed: the 2D posteriors are tighter for $P$ ($\sim$0.86$\times$) and $\rho$ ($\sim$0.70$\times$), while $U$ and $R$ are comparably or better constrained by 1D column counts, suggesting that spatial information provides additional constraining power for some parameters but not uniformly across all.
}

\DIFadd{More broadly, the cost-benefit calculation shifts decisively toward spatial approaches for models where crucial information is encoded in patterns that resist simple summarization: }\DIFaddend agent-agent \DIFdelbegin \DIFdel{interactions through exclusion effects, chemotaxis, or }\DIFdelend \DIFaddbegin \DIFadd{interactions, chemotaxis, }\DIFaddend cell-cell \DIFdelbegin \DIFdel{adhesion generate complex formations that are lost when compressed into simple one-dimensional projections. Similarly, heterogeneous environments with spatially varying growth rates, physical barriers, or chemical gradients create complex spatial signatures that contain crucial parameter information. Multi-species dynamics involving competition,predation, or cooperation also produce emergent spatial organization that encodes information about underlying mechanisms. Collective behaviours such as swarming, flocking, or tissue morphogenesis involve coordinated movements that are fundamentally spatial phenomena requiring spatial analysis for proper characterization.
In these more complex scenarios, CNN approaches would preserve information that would otherwise be discarded by traditional summary statistics. }\DIFdelend \DIFaddbegin \DIFadd{adhesion, heterogeneous environments, and multi-species dynamics all generate spatial structure that 1D projections discard. When the degenerate structure is known \emph{a~priori}, reparameterizing to identifiable continuum parameters (Section~\ref{sec:model_a}) provides an additional route to improved calibration beyond increasing the training budget or changing the data representation.
}\DIFaddend 

\DIFdelbegin \DIFdel{A critical aspect of applying deep learning models like CNNs in a scientific context is interpretability. While these networks are often treated as "black boxes", it is both possible and important to investigate the features they learn to solve a given inferenceproblem. In principle, techniques }\DIFdelend \DIFaddbegin \DIFadd{Interpretability remains an open challenge for CNN-based inference. Techniques }\DIFaddend such as activation mapping \cite{2023arXiv230914304M} or saliency maps \citep{2019arXiv191111293M} could \DIFdelbegin \DIFdel{be used to }\DIFdelend visualize the spatial features our CNN learns to associate with different parameter values. \DIFdelbegin \DIFdel{One might expect the network to develop }\DIFdelend \DIFaddbegin \DIFadd{The network likely develops }\DIFaddend sensitivity to biologically relevant patterns like the sharpness of the spreading front, local cell density gradients, or the degree of cell clustering. By identifying these learned features, a researcher could validate that the network's reasoning aligns with established biological theory, or discover novel spatial statistics that are informative but not immediately obvious to a human observer. While a detailed feature analysis is beyond the scope of this work, we highlight its importance for future applications. The ability to not only perform inference but also to extract the learned representations that enable it is a \DIFdelbegin \DIFdel{key }\DIFdelend \DIFaddbegin \DIFadd{distinct }\DIFaddend advantage of this approach.

The ability to perform inference with stochastic models on spatiotemporal data has direct implications for experimental design. Traditional protocols often reduce \DIFdelbegin \DIFdel{rich data from sources like }\DIFdelend \DIFaddbegin \DIFadd{spatiotemporal data from }\DIFaddend time-lapse microscopy to simplified measurements\DIFdelbegin \DIFdel{. This is a compromise driven by }\DIFdelend \DIFaddbegin \DIFadd{, largely because of }\DIFaddend past computational limits\DIFdelbegin \DIFdel{, not biological necessity}\DIFdelend . Our framework removes this barrier, enabling direct analysis that preserves the full spatial information. This should encourage the strategic collection and retention of rich datasets, which is crucial for improving parameter identifiability in complex systems where spatial structure encodes mechanistic information.

%DIF < Some notes on runs for reproductibility of runtimes
%DIF < workflow_20250919_083626 ::log file is reproduce_15547_2d_v3_4653638.txt
%DIF < ---> This is the 2D run (8 rounds)
\DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
%DIF < workflow_20250918_225411 :: log file reproduce_15547_2d_4651982.txt. 
%DIF < ---> This is also 2D (6 rounds)
%DIFDELCMD < 

%DIFDELCMD < %%%
%DIF < workflow_20250827_224139 :: snpe_random_walk_4013453.txt
%DIF < -->This is the 1D run
\DIFdelend \subsection{Computational Scalability and Future Directions}
\DIFdelbegin %DIFDELCMD < \label{sec:runtime}
%DIFDELCMD < %%%
\DIFdel{The NPE framework, particularly with the CNN-based spatial analysis, introduces several computational considerations that will influence its applicability to large-scale problems. To provide a practical comparison of these costs, Table~\ref{tab:runtimes} summarises representative runtimes for the different inference methods. These figures are illustrative of a typical workflow and are not the result of systematic hyper-parameter optimisation. For example, the NPE training time scales roughly linearly with the number of sequential rounds: the reported 1D case used ten rounds, so halving or doubling this number would proportionally reduce or increase the runtime, with an uninvestigated trade-off in posterior accuracy. Similarly, ABC runtime depends directly on the number of simulations generated (here, $10^5$). Other hyper-parameters (e.g. number of network epochs, batch size, learning rate,  width/depth of the embedding network) also have a direct influence on both runtime and accuracy, but were fixed to reasonable defaults rather than tuned for performance. We also note the difference in hardware: the computationally expensive NPE training phase was performed on a GPU, whereas the other methods ran on a standard CPU. }%DIFDELCMD < \newline 
%DIFDELCMD < %%%
\DIFdelend 

\DIFdelbegin %DIFDELCMD < \begin{table}[htbp]
%DIFDELCMD < 	\centering
%DIFDELCMD < 	\renewcommand{\arraystretch}{1.2} %%%
%DIF <  for better row spacing
	%DIFDELCMD < \begin{tabular}{@{}llcc@{}}
%DIFDELCMD < 		\toprule
%DIFDELCMD < 		%%%
\DIFdelFL{\textbf{Method} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{Model / Data Type} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{Training Time (s)} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{Inference Time (s)} }%DIFDELCMD < \\
%DIFDELCMD < 		\midrule
%DIFDELCMD < 		%%%
\DIFdelFL{ABC }%DIFDELCMD < & %%%
\DIFdelFL{Stochastic Simulator (1D Data) }%DIFDELCMD < & %%%
\DIFdelFL{-- }%DIFDELCMD < & %%%
\DIFdelFL{$\sim$1200 }%DIFDELCMD < \\
%DIFDELCMD < 		%%%
\DIFdelFL{ABC }%DIFDELCMD < & %%%
\DIFdelFL{Surrogate Model (1D Data) }%DIFDELCMD < & %%%
\DIFdelFL{-- }%DIFDELCMD < & %%%
\DIFdelFL{$\sim$2 }%DIFDELCMD < \\
%DIFDELCMD < 		%%%
\DIFdelFL{MCMC }%DIFDELCMD < & %%%
\DIFdelFL{Surrogate Model (1D Data) }%DIFDELCMD < & %%%
\DIFdelFL{-- }%DIFDELCMD < & %%%
\DIFdelFL{$\sim$1 }%DIFDELCMD < \\
%DIFDELCMD < 		\midrule
%DIFDELCMD < 		%%%
\DIFdelFL{\textbf{NPE} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{Stochastic Simulator (1D Data)} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{$\sim$6400} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{$<$ 0.1} }%DIFDELCMD < \\
%DIFDELCMD < 		%%%
\DIFdelFL{\textbf{NPE + CNN} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{Stochastic Simulator (2D Data)} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{$\sim$9800} }%DIFDELCMD < & %%%
\DIFdelFL{\textbf{$<$ 0.1} }%DIFDELCMD < \\
%DIFDELCMD < 		\bottomrule
%DIFDELCMD < 	\end{tabular}
%DIFDELCMD < 		%%%
%DIFDELCMD < \caption{%
{%DIFAUXCMD
\DIFdelFL{Representative computational runtimes for various inference methods. NPE requires a significant one-time training cost, after which inference is near-instantaneous. The NPE training was conducted on a GPU, while other methods were run on a CPU.}}	%DIFAUXCMD
%DIFDELCMD < \label{tab:runtimes}
%DIFDELCMD < \end{table}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{Two distinct computational strategies emerge from this comparison. Surrogate-based approaches are extremely fast because they entirely bypass repeated stochastic simulations, relying instead on precomputed or deterministic approximations of the model. In contrast, the NPE framework demands a substantial one-time computational investment during training but then performs posterior inference for new observations almost instantaneously. This amortised structure makes NPE particularly attractive for scenarios requiring repeated or real-time inference across many datasets. 
}%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend The primary cost for any NPE approach is the upfront simulation budget, which can become substantial for models with high-dimensional parameter spaces (\DIFdelbegin \DIFdel{$> 15-20$ }\DIFdelend \DIFaddbegin \DIFadd{$> 15$--$20$ }\DIFaddend parameters). On top of this, the CNN adds specific challenges\DIFdelbegin \DIFdel{; }\DIFdelend \DIFaddbegin \DIFadd{: }\DIFaddend (i) memory requirements scale quadratically with spatial resolution, potentially limiting application to very high-resolution imaging data without careful optimization; (ii) architecture selection for CNN embedding networks requires problem-specific tuning of depth, width, and kernel sizes, adding complexity to the analysis pipeline; (iii) the combined optimization of feature extraction and density estimation can be more challenging than simpler approaches, requiring careful hyper-parameter selection\DIFdelbegin \DIFdel{and potentially longer training times. }\DIFdelend \DIFaddbegin \DIFadd{. (We note that for the lattice sizes considered here, CNN training adds negligible overhead; this concern applies primarily to higher-resolution spatial data.)
}\DIFaddend 

\DIFdelbegin \DIFdel{Despite these challenges, several technological trends suggest that spatial inference approaches will become increasingly practical. Advances in GPU computing and specialized hardware for neural network training continue to reduce computational barriers.Improved network architectures and training algorithms may }\DIFdelend \DIFaddbegin \DIFadd{These costs are decreasing: GPU-accelerated simulators can }\DIFaddend reduce the simulation \DIFdelbegin \DIFdel{budgets required for reliable inference. Transfer learning approaches could enable }\DIFdelend \DIFaddbegin \DIFadd{bottleneck by orders of magnitude, improved flow architectures reduce the training budget needed for accurate posteriors, and transfer learning may allow }\DIFaddend pre-trained spatial feature extractors to be adapted to new biological problems \DIFdelbegin \DIFdel{with reduced computational cost. }%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdel{The framework developed here provides a foundation that can evolve with these technological advances while addressing increasingly complex biological questions. Future extensions might incorporate }\DIFdelend \DIFaddbegin \DIFadd{without retraining from scratch. Natural extensions of this framework include incorporating temporal dynamics from time-lapse microscopy (multiple snapshots rather than a single time point), }\DIFaddend three-dimensional spatial data \DIFdelbegin \DIFdel{, temporal dynamics, or multi-modal observations combining spatial patterns with biochemical measurements.
As biological modelling advances toward more realistic representations of complex systems, the ability to learn }\DIFdelend \DIFaddbegin \DIFadd{from confocal imaging, and multi-species models where inter-population interactions generate spatial structure that resists simple summarization.
}

\subsection{\DIFadd{Conclusions}}

\DIFadd{We have demonstrated that NPE provides a principled path beyond the long-standing tension between computational tractability and biological realism in parameter inference for stochastic agent-based models. Across a hierarchy of random walk models---from isotropic diffusion through to a four-parameter model combining directional bias and proliferation---NPE performs well regardless of whether a reliable surrogate happens to exist. Integrating CNNs as learned feature extractors enables inference }\DIFaddend directly from high-dimensional \DIFdelbegin \DIFdel{, spatially structured datawill become essential for extracting quantitative insights from biological observations}\DIFdelend \DIFaddbegin \DIFadd{spatial data, removing reliance on hand-crafted summary statistics. We provide an open-source implementation of our complete pipeline}\footnote{\href{https://github.com/tomkimpson/NPE_for_RW_inference}{\DIFadd{github.com/tomkimpson/NPE\_for\_RW\_inference}}} \DIFadd{to facilitate adoption and extension to more complex, spatially-structured biological models}\DIFaddend .

\DIFdelbegin \section{\DIFdel{Conclusion}}
%DIFAUXCMD
\addtocounter{section}{-1}%DIFAUXCMD
\DIFdelend \DIFaddbegin \newpage
\DIFaddend 


\DIFdelbegin \DIFdel{Parameter inference for spatially explicit stochastic biological models has long been hampered by a fundamental tension: the need for computational tractability versus the desire for biological realism. Classical approaches have required researchers to choose between computationally demanding likelihood-free methods that operate directly on stochastic simulators, or efficient likelihood-based techniques that rely on potentially unfaithful surrogate models and explicit noise specifications.
This work demonstrates that NPE provides a principled path beyond this dichotomy.
}\DIFdelend \DIFaddbegin \bibliographystyle{unsrt}
\bibliography{references}
\DIFaddend 

\DIFdelbegin \DIFdel{We have shown that NPE, augmented with modern deep learning architectures, addresses each of }\DIFdelend \DIFaddbegin \newpage
\appendix
\section{\DIFadd{Posterior Predictive Checks}}\label{sec:appendix_ppc}

\DIFadd{To complement the simulation-based calibration diagnostics presented in Section~\ref{sec:diagnostics}, we evaluate NPE's predictive capabilities through posterior predictive sampling for }\DIFaddend the \DIFdelbegin \DIFdel{canonical challenges that have historically limited stochastic biological modelling. By learning posteriors directly from simulator output, the framework circumvents intractable likelihoods while avoiding the systematic biases introduced by mean-field approximations. The method's implicit learning of complex stochastic relationships eliminates the need for }\DIFdelend \DIFaddbegin \DIFadd{original model. For each posterior sample $\theta^{(j)}$, we generate predicted data $\tilde{x}^{(j)} \sim p(x|\theta^{(j)})$ using the stochastic simulator. The collection of predictions forms empirical prediction intervals that naturally account for both parameter uncertainty and model stochasticity.
}

\subsection{\DIFadd{1D Column Count Predictions}}

\DIFadd{As shown in Figure~\ref{fig:npe_1d_predictive}, the resulting NPE-based predictions are well-calibrated, with 94.8\% of true observations falling within the 95\% prediction intervals. A key advantage of this simulator-native approach is its physical consistency; the prediction intervals respect the non-negative, discrete nature of the count data without requiring an }\DIFaddend explicit, and potentially erroneous, noise model\DIFdelbegin \DIFdel{specification. Once trained, the amortized nature of NPE provides near-instantaneous posterior estimates, overcoming the prohibitive per-inference costs of traditional likelihood-free methods. Perhaps most significantly, by integrating CNNs as learned feature extractors, the framework can process high-dimensional spatial data directly, removing the need for hand-crafted summary statistics that risk substantial information loss.
}\DIFdelend \DIFaddbegin \DIFadd{. The framework also naturally captures heteroscedastic uncertainty, with the prediction intervals appropriately widening in regions of high agent density where stochastic variability is greatest.
}\DIFaddend 

\DIFdelbegin \DIFdel{Our application to }\DIFdelend \DIFaddbegin \begin{figure}[htbp]
	\centering
	\includegraphics[width=0.8\textwidth]{images/example_predictive_plot_for_paper_1D}
	\caption{\DIFaddFL{Posterior predictive checks from NPE using 1D column count data. The observed data (orange circles) is overlaid on predictions generated by simulating the model with parameters drawn from the inferred posterior distribution. The median (solid red line) and mean (dashed red line) of the predictions are shown, along with the 50\% (dark blue) and 95\% (light blue) prediction intervals. For visualization purposes, a Gaussian kernel has been applied to smooth the predictive distributions. An appropriate proportion of the observed data points lie within the 95\% prediction interval, indicating that the calibrated model is consistent with the data.}}
	\label{fig:npe_1d_predictive}
\end{figure}

\subsection{\DIFadd{2D Spatial Predictions}}

\DIFadd{As shown in Figure~\ref{fig:npe_2d_predictive_cnn}, the 2D-trained model generates predictions consistent with the observed data. The calibrated model accurately captures the spatial characteristics of the }\DIFaddend agent-based \DIFdelbegin \DIFdel{random walk models demonstrates these advantages in practice. On one-dimensional summary statistics, NPE matches or exceeds the precision of classical methods while requiring no manual tuning of tolerances or distance metrics. When extended to raw two-dimensional spatial datathrough CNN-based feature extraction}\DIFdelend \DIFaddbegin \DIFadd{simulation, producing prediction intervals that are well-calibrated and physically consistent with the discrete nature of the data. Importantly}\DIFaddend , the framework \DIFdelbegin \DIFdel{maintains comparable precision without requiring researchers to specify informative summary statistics a priori. This capability becomes critical for complex biological systems where crucial mechanistic information is encoded in spatial patterns that resist simple summarization.}\DIFdelend \DIFaddbegin \DIFadd{captures the input-dependent (heteroscedastic) uncertainty, something that standard MLE under an i.i.d.\ noise assumption (used in much of the mathematical biology literature) cannot represent, often leading to miscalibrated, homoscedastic intervals even when the underlying variability is spatially varying. As in the 1D case, the method remains robust when applied to high-dimensional (2D) spatial inputs.
}\DIFaddend 

\DIFdelbegin \DIFdel{The broader implications extend beyond the specific models considered here. As biological research increasingly demands quantitative frameworks that can accommodate realistic stochasticity, spatial heterogeneity, and multi-scale dynamics, the ability to perform rigorous inference directly on high-fidelity simulators becomes essential.
The framework we have developed provides a foundation for this next generation of biological modelling, where machine learning techniques can bridge the gap between model complexity and inferential tractability.
}\DIFdelend \DIFaddbegin \DIFadd{In the 2D (CNN) results we also note a modest separation between the posterior predictive mean and median, consistent with a mild right-skew in the posterior for $D$. We attribute the observed skew primarily to structural features of the inference problem---namely a slight $U$--$D$ trade-off, support constraints ($D\!\ge\!0$ with $D\!=\!P/4$), and the non-linear parameter-to-observable map---together with the fact that the CNN accesses richer spatial cues (e.g., front roughness and local clustering) that are averaged out in 1D summaries and therefore make such asymmetries more apparent. We cannot entirely rule out minor artefacts arising from the higher-dimensional learning task.
}\DIFaddend 

\DIFdelbegin \DIFdel{Nevertheless, important challenges remain. The substantial upfront computational cost of generating training data, the difficulty of ensuring robust performance outside the training distribution, and the limited theoretical guarantees compared to classical methods all require careful consideration.
The architecture design choices and hyper-parameter tuning introduce additional complexity that may present barriers to adoption. Future work should focus on reducing these barriers through improved architectures, more efficient training algorithms, and the development of practical guidelines for applying these methods across diverse biological systems. }\DIFdelend \DIFaddbegin \begin{figure}[htbp]
	\centering
	\includegraphics[width=0.8\textwidth]{images/example_predictive_plot_for_paper_2D_CNN}
	\caption{\DIFaddFL{Posterior predictive check for the 2D spatial data model, showing the full observed data points (orange) overlaid on the predicted occupancy probability from the NPE model (blue heat-map). The grey contours delineate iso-probability curves at probability levels of 0.05, 0.10, and 0.20 (dotted, dashed, solid, respectively).}}
	\label{fig:npe_2d_predictive_cnn}
\end{figure}
\DIFaddend 

\DIFdelbegin \DIFdel{We provide an open-source implementation of our complete pipeline}\footnote{\href{https://github.com/tomkimpson/NPE_for_RW_inference}{\DIFdel{github.com/tomkimpson/NPE\_for\_RW\_inference}}%DIFAUXCMD
} %DIFAUXCMD
\addtocounter{footnote}{-1}%DIFAUXCMD
\DIFdel{to facilitate adoption and enable the community to extend these methods to increasingly complex spatially-structured models. As computational resources continue to expand and neural network architectures become more sophisticated, we anticipate that simulation-based inference frameworks like NPE will become a standard approach for parameter estimation in stochastic biological systems. }\DIFdelend \DIFaddbegin \section{\DIFadd{Reproducibility Across Random Seeds}}\label{sec:reproducibility}
\DIFaddend 

\DIFdelbegin %DIFDELCMD < \newpage
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \DIFadd{Neural network training involves stochastic elements---random weight initialisation and mini-batch ordering---that could in principle cause posterior estimates to vary between independent runs. To quantify this variability, we retrained the 1D NPE pipeline five times for each model using different random seeds (42, 123, 456, 789, 1024) while holding the test observation fixed. Table~\ref{tab:seed_study} summarises the results. Posterior mean estimates are stable across seeds: the standard deviation of the posterior mean is at most 0.032 ($P$ in Model~A), which is small relative to both the posterior width and the parameter range. Credible interval widths are similarly consistent, with relative fluctuations typically below 10\%. The true parameter value falls within the 95\% credible interval for every seed and every parameter, yielding 100\% empirical coverage. These results confirm that the trained posteriors are robust to the stochastic elements of the training procedure and that a single training run is sufficient for reliable inference. We observe similar stability for the 2D NPE posteriors across seeds.
}\DIFaddend 

\DIFdelbegin %DIFDELCMD < \bibliographystyle{plain}
%DIFDELCMD < \bibliography{references}  
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \begin{table}[htbp]
	\centering
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{@{}llcccc@{}}
		\toprule
		\DIFaddFL{\textbf{Model} }& \DIFaddFL{\textbf{Param} }& \DIFaddFL{\textbf{True} }& \DIFaddFL{\textbf{Post.\ mean (mean $\pm$ std)} }& \DIFaddFL{\textbf{CI width (mean $\pm$ std)} }& \DIFaddFL{\textbf{Coverage} }\\
		\midrule
		\multirow{2}{*}{Original} & \DIFaddFL{$U$ }& \DIFaddFL{0.300 }& \DIFaddFL{$0.2927 \pm 0.0010$ }& \DIFaddFL{$0.0349 \pm 0.0016$ }& \DIFaddFL{5/5 }\\
		                          & \DIFaddFL{$P$ }& \DIFaddFL{0.700 }& \DIFaddFL{$0.6632 \pm 0.0078$ }& \DIFaddFL{$0.3811 \pm 0.0114$ }& \DIFaddFL{5/5 }\\
		\midrule
		\multirow{3}{*}{A (Bias)} & \DIFaddFL{$U$    }& \DIFaddFL{0.500 }& \DIFaddFL{$0.4978 \pm 0.0012$ }& \DIFaddFL{$0.0401 \pm 0.0016$ }& \DIFaddFL{5/5 }\\
		                          & \DIFaddFL{$P$    }& \DIFaddFL{0.700 }& \DIFaddFL{$0.6531 \pm 0.0319$ }& \DIFaddFL{$0.5406 \pm 0.0125$ }& \DIFaddFL{5/5 }\\
		                          & \DIFaddFL{$\rho$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.5613 \pm 0.0314$ }& \DIFaddFL{$0.5621 \pm 0.0148$ }& \DIFaddFL{5/5 }\\
		\midrule
		\multirow{3}{*}{B (Prolif.)} & \DIFaddFL{$U$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.3464 \pm 0.0159$ }& \DIFaddFL{$0.4789 \pm 0.0260$ }& \DIFaddFL{5/5 }\\
		                              & \DIFaddFL{$P$ }& \DIFaddFL{0.700 }& \DIFaddFL{$0.7615 \pm 0.0050$ }& \DIFaddFL{$0.3313 \pm 0.0044$ }& \DIFaddFL{5/5 }\\
		                              & \DIFaddFL{$R$ }& \DIFaddFL{0.050 }& \DIFaddFL{$0.0533 \pm 0.0008$ }& \DIFaddFL{$0.0223 \pm 0.0006$ }& \DIFaddFL{5/5 }\\
		\midrule
		\multirow{4}{*}{C (Bias + Prolif.)} & \DIFaddFL{$U$    }& \DIFaddFL{0.500 }& \DIFaddFL{$0.4809 \pm 0.0259$ }& \DIFaddFL{$0.4315 \pm 0.0176$ }& \DIFaddFL{5/5 }\\
		                                    & \DIFaddFL{$P$    }& \DIFaddFL{0.700 }& \DIFaddFL{$0.6832 \pm 0.0123$ }& \DIFaddFL{$0.3038 \pm 0.0112$ }& \DIFaddFL{5/5 }\\
		                                    & \DIFaddFL{$\rho$ }& \DIFaddFL{0.500 }& \DIFaddFL{$0.4753 \pm 0.0091$ }& \DIFaddFL{$0.2178 \pm 0.0054$ }& \DIFaddFL{5/5 }\\
		                                    & \DIFaddFL{$R$    }& \DIFaddFL{0.050 }& \DIFaddFL{$0.0521 \pm 0.0003$ }& \DIFaddFL{$0.0143 \pm 0.0003$ }& \DIFaddFL{5/5 }\\
		\bottomrule
	\end{tabular}
	\caption{\DIFaddFL{Reproducibility across random seeds. For each model and parameter, we report the mean $\pm$ standard deviation of the posterior mean and the 95\% credible interval width across 5 independent training runs. Coverage indicates the fraction of seeds for which the true value falls within the 95\% credible interval. All 1D NPE with 50}{\DIFaddFL{,}}\DIFaddFL{000 training simulations.}}
	\label{tab:seed_study}
\end{table}
\DIFaddend 

\end{document}
