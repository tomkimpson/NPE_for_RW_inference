Note that `.pkl` results files in this directory are typically large and are not tracked by version control.

Please use the provided `extract_results.py` script for compressed outputs for plotting posteriors.


Note: we should handle the IO at the end of main.py better to avoid an extra extract_results step.
